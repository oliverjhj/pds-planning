# STATE.md
# Passenger Display System (PDS) — Current State

**This is the living snapshot of the project. It is the current authority for what is built, what remains, and what is being worked on. It is overwritten at the end of each working session to always reflect reality.** Session-by-session history is append-only in `pds-planning/sessions/`. The narrative history up to the July 2026 workflow migration is in `pds-planning/historical/PROJECT-HISTORY.md`. The original planning documents (frozen, historical) are alongside it in `pds-planning/historical/`.

**Last updated:** July 2026 (2026-07-30, twelfth session — **Phase 2b executed: this repository's entire git history was rewritten and republished. PHASE 2 IS COMPLETE.** All 59 commits were rewritten with `git-filter-repo` v2.47.0 to remove private identifiers before the repo goes public — the pilot customer's relationship to the owner, the pilot's location and route data (place names, two route numbers, a NaPTAN area prefix), both Firebase project IDs, the owner's personal email, and the local Windows username — each replaced with a fictional equivalent chosen to keep the prose coherent and the technical record intact. **The substitution table lives in the workspace `CLAUDE.md`, which is untracked and never published; it is deliberately not recorded in this repo, because publishing the mapping would reverse the scrub.** Scope was widened mid-plan on evidence: 2a had scoped 2b to the relationship alone because the company is "never named", but a fresh audit found the pilot identifiable by **location**, so naming-only would have left the job half done; the owner's own home town was scrubbed on a second pass for the same reason. **The GitHub repo was deleted and recreated, never force-pushed** — a force-push leaves pre-scrub commits fetchable by SHA indefinitely. Old HEAD `2c9ae69` → new HEAD `18d746d`; **every SHA changed**, so short `pds-planning` hashes quoted in earlier session files and in this ledger no longer resolve (cross-repo `pds-android`/`pds-dashboard` hashes are unaffected). Verified: all 26 scrubbed patterns zero across every commit *and* across a fresh clone from GitHub; author/committer names, emails and both dates byte-identical across all 59 commits (the dated timeline is the exhibit); all 59 subjects identical with exactly six bodies changed; false positives and out-of-scope items intact at exact pre-measured baselines. Ledger #45. Session file: `2026-07-30-portfolio-conversion-phase-2b-history-scrub.md`. **Next: the mock-GPS track, then Phase 3 capture.**)

**Prior 2026-07-29 (eleventh session) — **the portfolio conversion begins executing; Phase 1 and Phase 2a are done.** Work is now driven by the owner's vault plan (`Documents\brain`, `projects/pds-cv/conversion-plan.md` + `shot-list.md`), which this repo follows rather than owns. **Phase 1 (polish only what is on camera):** the landing page's two remaining placeholders are gone — `£—` became **"Pricing on application"** (section kept, `/ vehicle / month` span removed with it, typography stepped off mono per Rule 16) and the owner-contact placeholder comment was **deleted rather than filled**, since an address on a page about to be decommissioned dies with the domain while persisting in archives. Two `pds-dashboard` commits `8817749`→`e4cd749`, owner-reviewed, pushed, Vercel-deployed and live-verified in both themes. The **session-aware landing header was exercised in production for the first time** (owner-verified `Sign up` → `Open dashboard` → `/routes`). **Phase 2a (scrub the data that appears on camera):** production is now fictional. The pilot operator's real **Wellcastle** route (`X12` "Marford Green to Guildhall Square", 6 real `9990`-prefix NaPTAN stops), its 29 journey summaries, 11 pairing codes, 26 rate-limit rows and all **21 audio WAVs** are deleted; the two third-party test-operator accounts — whose real personal Gmail addresses, not the company names, were the actual exposure — were deleted via the auth admin API and re-created as fictional operators (**Coach Company Ltd**, **Rail Replacement Ltd**, alongside the retained **Bus Company Ltd**). The demo route **RR1 · Victoria to Paddington** was authored through the real route builder on six genuine London NaPTAN stops and rendered clean (`ok`, `audio_version` stamped, 7 WAVs), composing to *"This is the RR1 service to Paddington Station."* — matching the landing page's own example. The tablet was uninstalled **before** the deletes (its pending-summary outbox would otherwise have re-uploaded real journeys afterwards), then reinstalled on `prodDebug` and re-paired; `audio_enabled` + `fcm_token` both confirmed, RR1 owner-glass-confirmed in the route list. Ledgers #43–44. **Phase 2b (the `pds-planning` git-history scrub) is audited and planned but deliberately NOT started** — it is the only irreversible step in the conversion and gets its own session. Session file: `2026-07-29-portfolio-conversion-phase-1-and-2a.md`.)

**Prior 2026-07-26 (tenth session) — **THE BUSINESS IS CLOSED; PDS pivots to a portfolio piece.** The session's first half shipped the last commercial-track work: the **`/hardware` public hardware guide** (page-over-PDF, owner-decided; July-2026 market research, three tablet tiers, mounts/power/speakers, ~£240-per-vehicle cost strip, print CSS as the dated-snapshot mechanism) plus a run of landing polish — nav reorder ("Hardware guide" trails the hash anchors), the wordmark-always-returns-to-landing rule, the site-wide muted top progress bar (nextjs-toploader), and the TabletDemo reworked onto the shared London **RR1 → Paddington Station** example route (also now `/guide`'s SAMPLE and the ticker-simulator defaults) with bigger text, loop-opens-at-stop-1, reset-under-the-terminating-overlay, and a descender fix. Ten `pds-dashboard` commits `d21ccea`→`91c298c`, all deployed and live-verified. A full pricing/legal/company plan was designed the same day (graduated per-device pricing, no-card trial, Ltd + Stripe sequencing — recorded in the session file) and then **superseded within 24 hours: the pilot customer (a personal contact's bus company) backtracked**, and the owner closed the commercial track entirely — no incorporation, no payment gate, no sales effort; nothing was built that needs unwinding. The deployed product stays live as the portfolio exhibit. Ledger #42. Remaining work (scrub → README/story → public repos) is owner-paced and tracked OUTSIDE this workspace in the owner's new personal vault (`Documents\brain`, `projects/pds-cv/`). Session file: `2026-07-26-hardware-guide-landing-polish-and-business-closure.md`.)

**Prior 2026-07-23 (ninth session) — **Phase 4 opened: the public landing page is live on `pds-dashboard.com`.** `/` is now a standalone public marketing page — owner-decided "Model A": shop window vs workshop, the dashboard gated exactly as before, the landing header **session-aware** ("Sign up" for visitors / "Open dashboard" for a logged-in operator, no forced redirect). Built as a new **`(marketing)` route group** (layout + header/footer + page) so the Phase 4 privacy/terms pages inherit the chrome; `'/'` removed from `DASHBOARD_PATHS` (comment rewritten), the `(auth)` layout's logged-in redirect retargeted to `/routes`. The hero centrepiece is the **live tablet demo** (owner-picked over generic 3D flair — the product visibly working IS the pitch): `TabletDemo`, a self-driving ~21 s looping journey — route line via `@shared/announcements`, hero advancing five stops, stop rail progressing, terminating announcement as the 500 ms flash inversion + overlay — the documented THIRD sibling of the tablet-depiction family (deliberately not unified; overflow-measured marquee, no physical-mm claims; pauses off-screen; static frame under reduced motion; carries the third documented copy of the terminating wording). Sections: problem → BYO-tablets proposal → four steps on the stop-rail idiom → plain-English compliance → fleet grid → pricing placeholder (`£—`, owner supplies the figure) → FAQ → CTA. Two new theme-invariant `--tablet-flash-*` tokens (depiction extension — values from the design language's flash row, no ledger-#27 sync). Metadata/robots/sitemap added; OG image deferred (wants a designed asset off the pinned-screen mark). Three `pds-dashboard` commits `2c70663`→`433a3dd`→`358c5ee`, owner-reviewed locally then pushed at owner instruction; Vercel deployment READY for the SHA and **live-verified**: anonymous `/` 200 with content, `/routes`/`/guide` still 307 → `/login`, robots + sitemap serving. Ledger #41. Session file: `2026-07-23-public-landing-page.md`. **Next: the rest of Phase 4 — hardware-recommendation doc, legal drafts, pitch pack, Play Store research, pricing model (fills the landing placeholder).**)

**Prior 2026-07-23 (eighth session) — Phase 3 closed: the ticker/marquee simulator, plus the design-language interaction-affordance section.** Shipped the second and final Phase 3 item — a `/guide` authoring aid ("Will my route and stop names fit?") that, for a chosen tablet screen size, shows whether the passenger route line and next-stop hero sit static or **marquee**, mirroring the tablet's `PhysicalTextMath.tickerPlan`. **Load-bearing insight:** the marquee decision needs only the physical screen WIDTH — resolution cancels, because text and screen both scale by the same px/mm — so the tool asks for diagonal + aspect ratio, never DPI. Built as a pure, unit-tested `src/lib/ticker-sim.ts` (12 tests; ticker constants + `HERO_CANDIDATE_MM` copied from the Android repo with a source-of-truth citation, same posture as the fixed-announcement strings in `guide/page.tsx`), a new higher-fidelity `TabletTickerScreen` depiction (measured + marqueeing, deliberately distinct from the truncating `TabletRouteLineMock`, which is untouched), and a `TickerSimulator` client island doing live in-browser Inter cap-height/width measurement (canvas, mirroring the tablet's technique) + the hero rung step-down. Route-line wording reuses `@shared/announcements` so it can't drift; near the boundary the verdict reads "borderline" (browser vs Android metrics differ ~1–2%), and a 4%-per-side inset + ±3% band err toward warning. Scoped to sideways overflow only — the height-driven `PassengerTierMath` degradation is deliberately OUT and captioned. **Owner cross-checked against the real Lenovo TB373FU: the marquee/static boundary matches the tablet — perfect, no calibration change.** One `pds-dashboard` commit `bfbfc94`, pushed and Vercel-deployed to prod (deployment MCP-confirmed for the SHA). Then, a small design-doc task: added a **"Interaction & affordance"** section to the design language — the *principle* behind the cursor rule (affordance/focus-visible/disabled, plus the clickable-`<div>`-with-no-`role` gap the CSS structurally can't reach) — synced across all three ledger-#27 copies: the skill, `DESIGN-LANGUAGE.md` (bodies verified byte-identical), and a new `interaction.html` Claude Design card (default/hover/focus/pressed/disabled, both themes; project now **14 cards**, and a stale "12 cards" count corrected in both docs). No Rule 16/20 change — the principle generalises existing rules. Workspace-root `.playwright-mcp/` confirmed already clean (no-op). Session file: `2026-07-23-ticker-simulator-and-interaction-affordance.md`. **Next: Phase 4 — public landing page, hardware-recommendation doc, legal drafts, pitch pack, Play Store research, pricing model.**

**Prior 2026-07-21 (seventh session) — cursor affordance consistency across the dashboard.** A short, self-contained session, no feature work. The owner asked whether the hand-cursor-on-some-things-but-not-others was a deliberate button-vs-navigation distinction or inconsistency; it was inconsistency, and the diagnosis mattered more than the fix — **nothing in the stack sets a cursor for interactive elements at all** (Tailwind v4 preflight, `shadcn/tailwind.css` and `globals.css` all verified to leave `button` alone), so everything fell back to the UA stylesheet's `a[href]`-only rule. Symptom: identical-looking neighbours disagreeing — "New Route" (a `Link` wearing `buttonVariants`) got the hand, "Re-render all" beside it did not — plus three ad-hoc `cursor-pointer` patches already accumulated against the same root cause. Fixed centrally in `globals.css`, never in generated `ui/*` (Rule 16): a **base-layer** rule (`button`, `[role="switch"]`, radio/checkbox, `label:has(> input)`) whose layer placement is load-bearing — utilities outrank base, so `cursor-grab` and `cursor-default` still win — plus a deliberately **unlayered** `[data-slot="dropdown-menu-*"]` rule, the only way past the generated component's hard-coded `cursor-default` **utility** (owner-decided: menu items are actions, not OS-menu rows). Checking base-ui's actual rendered elements in `node_modules` is what caught `Switch` → `<span role="switch">` and `MenuItem` → `<div role="menuitem">`; a `button`-only selector would have silently missed the fleet audio toggle and every menu action. One reverse carve-out: the two hover-only `TooltipTrigger`s in `device-table.tsx` render as real `<button>`s and take an explicit `cursor-default`. Verified by compiled-stylesheet cascade inspection **and** a 12-shape runtime computed-style pass (including the four that must NOT change and three disabled states); deployed and **owner-confirmed on the live site**. Deliberately **not** synced to the three design-system copies — cursor behaviour cannot drift, it is dashboard-only, and a static Design card cannot show it; Rule 16 is the home instead. Two `pds-dashboard` commits `7291ae0`→`7935e9c`, both pushed and deployed. Also cleaned a stray workspace-root `.playwright-mcp/` (24 files accumulated since 11 July). Session file: `2026-07-21-cursor-affordance-consistency.md`. **Next: still the Phase 3 ticker/marquee simulator.**

**Prior 2026-07-20 (sixth session) — force re-render audio, routes UI polish, and the tablet sidebar.** Cleared both banked re-render items: staging's 4 routes confirmed re-rendered, and the Supabase CLI relinked prod → **staging** (safer resting state; it was on prod at start — ledger #39). Built **force re-render** — `triggerReRender` generalised to run on `ok` as well as `failed` routes, plus a new fleet-wide `bulkReRender` — entirely in the dashboard app layer, no Edge Function/migration/worker change. The mechanism is one owner-scoped RLS UPDATE (`status='pending'`, `error=null`, `audio_announcement_hash=null`): nulling the hash forces full synthesis so a **voice** change actually re-renders, and the `routes_updated_at` trigger bumps `updated_at` so a new `audio_version` is minted (dodges the ledger-#39 collision trap); `bulkReRender` skips in-flight `pending` routes (ledger #40). Deployed to prod, **owner glass-verified on the Lenovo AND backend-confirmed** (route X12 `audio_version` rose, hash re-stamped). Also shipped: **live render-status polling** (`RenderStatusPoller` — `router.refresh()` on a 4 s timer while any route is pending, no more manual refresh) and status display fixes ("Ready" on the routes list, detail render-status + button side by side in its own metadata-bar slot, "Ready" white on detail). And the Lenovo **ZUI freeform sidebar disabled** at device-settings level (like the pen button — no app machinery; the real fleet fix is Device Owner/MDM kiosk provisioning, promoted into Phase 6). Five `pds-dashboard` commits `a2987d2`→`9cf225c`, all pushed and deployed. Session file: `2026-07-20-force-rerender-and-routes-ui-polish.md`. **Next: the Phase 3 ticker/marquee simulator.**

**Prior 2026-07-18 (fifth session) — the passenger route-identity split, and Phase 3's first half.** `routes.display_name` became the operator's INTERNAL handle; `routes.route_number` the passenger-facing **designation** carrying Reg 7(1)(a) (free text, mandatory, migration 026). One template both surfaces: screen `"{designation} service to {final stop}"`, audio `"This is the {designation} service to {final stop}."`, final stop derived from the last stop — killing a doubled-"to" content bug. Wording single-sourced in `_shared/announcements.ts`, imported by BOTH the Deno worker and Next (ledger #37). Shipped with a **live route-builder preview** and the **`/guide` operator page** (Phase 3's wording item), the banner route number removed (ledger #38), and the Supabase-CLI footgun recorded (ledger #39). Session file: `2026-07-18-route-designation-and-operator-guide.md`.

**Prior 2026-07-18 sessions —** **Phase 2 items 1–3 SHIPPED** (11 `pds-android` commits, all glass-verified and pushed through `a94fd92`): the volume system rebuilt on diagnosed reality (a11y stream app-unwritable + aliased to media — slider AND key interception removed, the Reg 13 floor made real via the STREAM_MUSIC announcement-time raise; ledger #33 supersedes #10), the driver panel's fixed four-row grid + the **trigger-announcement menu** (supersedes the FR-AT-41 repeat-last Defer entry; ledger #34), the app-wide normal-width button pass, and a kiosk `singleTask` fix for the calibration-exit dual-instance race. **Phase 2's last named item — the tube-map transition visual — is the next action.** Second session same day: the theme-toggle "flashes out of time" artefact fixed on BOTH surfaces — dashboard `4cfce64` (canvas gradient moved off `background-attachment: fixed` onto a `body::before` underlay so `backdrop-filter` glass panels flip in-frame; pushed, live-verified on the domain) and android `e20fe6d` (transparent theme-invariant TopAppBars — M3's internal app-bar colour animation was fading the bar ~190 ms behind the atomic switch; diagnosed by screenrecord frame-stepping on the Lenovo; **glass check pending at next install**). Lenovo pen floating button hidden system-wide on the dev tablet, owner-decided. **Third session same day: the product logo.** A 50-option exploration converged on the owner-picked **A13 "pinned screen"** mark (the display with a location pin knocked out) — now the Android adaptive launcher icon (`c80ea15`) and, as a pin-in-circle favicon-fit variant, the dashboard favicon (`5c9a6b8`, live-verified on the domain); dead legacy launcher webps removed (`4a8ecd4`); ledger #35, supersedes #32's identity-resources line. Launcher icon awaits its glass look at the next install.)

---

## What PDS Is

A legally-regulated (UK PSVAR 2023), two-surface product: an **Android tablet app** (`pds-android`) that gives compliant audio + visual passenger information on rail-replacement buses, and a **Next.js dashboard** (`pds-dashboard`) where operators author routes and manage their tablet fleet. Both share a **Supabase** backend; audio is pre-rendered server-side via Google Cloud TTS. Pilot customer: a small bus company. See `PROJECT-HISTORY.md` for how it was built.

---

## Current State — What Is Built

The product is **functionally near-complete on both surfaces**; the original build stage closed 2026-07-10 (owner sign-off) and **no glass debt remains**. Everything below is built and hardware-verified unless noted. Since the 2026-07-17 roadmap consolidation this section is deliberately a summary — full session-by-session build detail (including commit-by-commit stacks) is append-only in `sessions/`, and load-bearing decisions live in the Decision Ledger below.

**Dashboard (`pds-dashboard`) — complete and live in production at `pds-dashboard.com`** (Vercel custom domain; genuinely publicly serving and operator-usable since 2026-07-12: prod admin account bootstrapped, auth-email flows on the domain and glass-verified, PDS-branded sender via Resend SMTP, Deployment Protection Standard). Built: **open signup** (migration 025 — new operators land `active`; the pending/suspended machinery retained, ledger #28); the **route builder** (full-UK NaPTAN stop search, per-stop proximity radius + segment type, return-route generation, live **geofence stop-spacing warnings** — severe consecutive-overlap + duplicate-coordinate advisory, `e746638`, haversine mirrored from the tablet so both surfaces judge distance identically); route list with render status (auto-updating "Ready"/"Rendering…" via the `RenderStatusPoller` client poll — `router.refresh()` while any route is `pending`); **force re-render audio** — per-route (any `ok`/`failed` route) and fleet-wide "Re-render all", app-layer only via the null-hash + version-bump mechanism (ledger #40); **fleet/device view** (live `active_route_id` column — request-time rendered, needs a refresh; per-device journey-summary drill-down); **account management** including the fixed password-reset flow (`79ad7f5`) and password-confirmed **self-service account deletion** (hard-wipe via the `delete-account` Edge Function, ledger #29); Web Analytics (cookieless; Speed Insights deliberately skipped); the green-circle favicon; the **`/guide` operator page** and the route builder's **live passenger-line preview** (ledger #37); the `/guide` **ticker/marquee simulator** (Phase 3, 2026-07-23 — physical-width-driven "will this name scroll?" authoring aid, `bfbfc94`); and the **public marketing landing page at `/`** (Phase 4, 2026-07-23 — `(marketing)` route group, session-aware header, live `TabletDemo` hero, robots/sitemap; ledger #41, `358c5ee`). The server-side **audio render pipeline** (pg_boss render-then-notify, version-keyed storage, differential re-render) is live. Dashboard Sentry is live with zeroed sample rates; `npm run lint` fully green. Session detail: the dated 2026-07-12 files in `sessions/`.

**Backend (Supabase).** Staging + production in verified lockstep: **migrations through 026** applied and artefact-verified on both environments (026 verified post-apply via `pg_constraint` on both); all **9 Edge Functions** deployed from HEAD to both (one accepted comment-only divergence on `pair-device`, `b0538ba`), every function carrying error-only **Sentry telemetry** through the shared `_shared/` modules with the vitest-tested NFR-R-07 scrubber (ledgers #20–21); Sentry quota guarded (spike protection on all three projects + the `edge-functions error flood` monitor, email delivery proven). Operator-scoped RLS, the pairing/recovery/pairing-code Edge Functions, the pg_boss render pipeline, route-audio Storage, NaPTAN full-text search, and `journey_summaries`. **Firebase is split per environment** (staging `pds-staging-xxxxx` / prod `pds-production-xxxxxx`, each Supabase project holding its own `FCM_SERVICE_ACCOUNT_JSON` key) — glass-verified end-to-end: dashboard save → render → FCM push → hands-free tablet sync.

**Android (`pds-android`) — feature-complete.** Pairing with encrypted credential storage (immediate first sync + FCM registration on pairing, `1377ac7`); reactive JWT recovery and the lifecycle heartbeat; the full **sync engine** (route/stop/audio download, three triggers, hands-free dashboard→tablet propagation, stage-7 `active_route_id` reporting with journey-seam sync triggers); **GPS stop detection on the announce-on-departure model with temporal hysteresis and the coarse-GPS "Weak GPS" warning** (ledger #31; `46697b2` / `bc70f73` / `6d26f78` — all glass-verified, including on the exact looping route that previously misfired); all automatic announcements (route, next-stop, termination, H&R with Reg 11-compliant timing and "approaching" wording, diversions with author/observe, GPS auto-skip, replay-on-resume) with the alert chime, co-equal screen flash, and text overlay; **≥22 mm physically-calibrated passenger text via the ticker/tier system** (single-line horizontal tickers + layout-only degradation tiers, ledger #26 — including the Reg 7(1)(a)+(b)(i) `"{designation} service to {final stop}"` carrier, ledger #37, and Tier C's Reg 10(1) carrier); the tube-map progress view (with the ledger-#36 at-stop / in-transit states — filled disc where the bus is, heavier highlighted segment while moving); journey completion/termination; the **driver control panel** (fixed four-row grid with the persistent diversion pair and the trigger-announcement menu — ledger #34; native volume keys with the FR-AT-44 floor enforced solely by the announcement-time STREAM_MUSIC raise — ledger #33, supersedes #10; Emergency Mute, manual H&R fallbacks, speaker-disconnect + GPS-lost + weak-GPS amber markers, solid-fill button styling — ledgers #9–19, #24, #30); Level 1 kiosk (screen pinning + default launcher); **admin PIN (4-digit minimum, `d6f056f`)** and the full admin menu; and post-journey summary upload. The two July Sentry crashes are fixed (resolved in Sentry) and the real Rule-17 PII scrubber is in place (no longer a stub).

**Both surfaces wear the "Steelglass" visual identity** (ledger #32, owner-signed-off and glass-verified 2026-07-17 — supersedes Booking Hall's visuals): strictly neutral monochrome, D2 near-black default / L1 pure-white light, 160° canvas gradient + translucent glass panels with hairline borders and inset top-highlight, Inter (UI) + JetBrains Mono (data), colour intent-only (ok green / semantic amber / failure red / terminal crimson). Dashboard Rule 16 / Android Rule 20 rewritten; three design-system copies synced per ledger #27 (the Claude Design project's 12 cards fully replaced 2026-07-17; `chips`/`glass-panel`/`mono-data` cards replace `stamps`/`ticket-table`/`ephemera`).

**Current `main` HEADs:** `pds-android` **`41980e1`**, `pds-dashboard` **`e4cd749`** — **both pushed**, working trees clean. The Android `CLAUDE.md` is current; the dashboard's stamp still cites `7291ae0` and now trails the landing-page work (the `(marketing)` group, public `/`, and the demo's third wording copy — refresh banked, see Bank and Assess); the frozen v3.9 planning docs are single-homed in `pds-planning/historical/`. Full commit-by-commit stacks live in the dated `sessions/` files.

---

## What's Left To Do — The Shipping Roadmap

> **BUSINESS CLOSED 2026-07-26 (tenth session; ledger #42).** The pilot customer withdrew and the owner ended the commercial track. The phases below are preserved as the record of the plan; everything not already shipped is **cancelled**, not pending. The only remaining PDS work is the **portfolio conversion**, now a video-first plan owned by the vault (`Documents\brain`, `projects/pds-cv/conversion-plan.md` + `shot-list.md`), not by this roadmap. Progress as of 2026-07-29: **Phase 0 (extract from history) done · Phase 1 (on-camera polish) done · Phase 2a (production data scrub) done · Phase 2b (this repo's git-history scrub) NEXT and not started · Phase 3 (capture) · Phase 4 (the edit gate) · Phase 5 (teardown) · Phase 6 (publish).** Recording is blocked only by the **mock-GPS track**; 2b blocks publishing, not filming. The deployed site stays live as the exhibit until Phase 5.

**Rewritten 2026-07-17 (third session): the owner-requested roadmap consolidation.** The owner's running to-do list is fully intaken below (the external list can be cleared); this section is now the single execution plan for the push to shipping. The old numbered build items are retired: **items 1–6 are complete** (history in `sessions/`), **item 7 is now Phase 5**, and the **item-8 raw intake is dissolved into the phases**. Repo `CLAUDE.md` citations of "item N" refer to that retired numbering — still valid as historical reference; don't renumber them.

**Sequencing logic (owner-set 2026-07-17):** design first — it gates every screenshot-bearing artefact, and the owner wants both surfaces polished and working before any commercial work; then polish/fixes, then the public face and documentation, then payment (deliberately late). The bus company wants the pilot "asap" but there is no exact timeline — a no-rush posture; Phase 6 slides earlier the moment their calendar firms up (it does not depend on Phases 4–5).

### Phase 1 — Full design redo — **COMPLETE 2026-07-17 (fourth session)**

**Shipped as "Steelglass" (ledger #32).** Three artifact rounds (20 directions → 10 hybrids → 10 shade variants) converged on the owner-picked pair: D2 near-black dark (default, both surfaces) + L1 pure-white light, Inter + JetBrains Mono. Implemented as dashboard commits D1–D7 and Android commits A1–A5 (stacks in the session file), owner glass-verified on the Lenovo, both repos pushed. Everything the phase required travelled with it: Rule 16/20 rewritten, `PassengerPaletteTest` extended (new `backgroundDeep` guard), three-way design-system sync re-run, visual halves of #25/#26/#30 recorded as superseded, compliance invariants untouched (cap-height machinery re-measured Inter automatically). The open sub-questions resolved: the two-theme toggle **survives** (dark default on both surfaces — the dashboard flipped from light-default); the warning role **stays semantic amber** in both themes; Gelasio **retired** for Inter.

### Phase 2 — Product polish & fixes — **COMPLETE 2026-07-18**

The owner's tablet polish notes, intaken 2026-07-17. **All four named items shipped**: items 1–3 on 2026-07-18 (session file: `2026-07-18-volume-system-driver-panel-and-admin-polish.md`), the tube-map visual later the same day (`2026-07-18-tube-map-transition-visual.md`). Only the phase's open-ended "whatever else owner-led testing surfaces" line remains, as designed.
- ~~**Volume slider**~~ — **DONE, resolved by diagnosis + removal**: the slider (and the whole key-time clamp) could never work — Android silently drops app writes to STREAM_ACCESSIBILITY — and that stream is platform-aliased to STREAM_MUSIC. Native volume keys everywhere; the floor is now real, enforced solely by the announcement-time STREAM_MUSIC raise (ledger #33; two recorded FR-AT-44 divergences).
- ~~**Diversion start/end button layout**~~ — **DONE**: fixed four-row grid, persistent diversion pair (relabel-in-place left, disabled-when-inactive End). Bonus: the **trigger-announcement menu** landed in the fourth row (ledger #34, supersedes the FR-AT-41 Defer entry).
- ~~**Button sizing/layout pass**~~ — **DONE app-wide**: `StandardControlWidth` (280 dp) across admin takeovers, driver overlays, calibration, journey-end, Start Journey; outlined secondaries in the existing default-grey idiom (deliberately NOT a design-system change); admin body copy capped at a centred 420 dp measure; calibration screen restyled (bottom Back, no top bar, 0.3 spawn fraction). Also fixed en route: the kiosk dual-instance calibration-exit race (`singleTask` + instance-safe reopen guard, `62c4f13`).
- ~~**Tube-map transition visual**~~ — **DONE 2026-07-18, glass-verified** (ledger #36). Shipped as a *static* three-state read from the existing departure gate: enlarged solid disc at the stop the bus is at, heavier highlighted segment while moving, plain upcoming nodes ahead. The originally-sketched moving dashed lines were rejected (Rule 20 reserves dashes for hail-and-ride), as was a separate 5–10 m "at the stop" radius (Rule 6's accuracy gate would have discarded nearly every real fix). Stop-name semantics and the ledger-#31 detection logic are untouched, as specified.
- Whatever else owner-led iterative testing surfaces along the way.

### Phase 3 — Design-dependent dashboard authoring aids — **COMPLETE 2026-07-23**

Deliberately blocked on Phases 1–2 (they bake in screenshots and rendered behaviour):
- ~~**Announcement-wording guidance on the route-authoring page**~~ — **DONE 2026-07-18**, shipped with the route-designation split (ledger #37) because free-text designations are only safe if their effect is visible while authoring. Two surfaces, deliberately both: a **live preview in the route builder** (the literal route line + the literal spoken sentence, composed by the *same* functions the render worker runs, so it cannot drift) and the **`/guide` page** (every announcement with its literal text, trigger and location; where each authored field lands on the tablet, including that the internal route name lands nowhere; what the operator controls vs what the regulations fix; and the save→Ready→download flow). Screenshots were rejected in favour of an HTML/CSS tablet mock — it stays current and can be driven live by form values.
- ~~**Ticker/marquee simulator**~~ — **DONE 2026-07-23, owner-cross-checked on the Lenovo.** Shipped on `/guide` ("Will my route and stop names fit?"): pick a tablet screen size (presets + custom), type the on-screen values (designation + final stop for the route line, a stop name for the hero), and it shows a faithful mock — static when it fits, **marqueeing** when it overflows — with a per-line Fits/Borderline/Scrolls verdict. Mirrors the tablet's `tickerPlan`/`PhysicalTextMath` (the ticker constants + `HERO_CANDIDATE_MM` are a cited cross-repo copy). **The decision needs only the physical screen WIDTH — resolution cancels (text and screen scale by the same px/mm)** — so it asks for diagonal + aspect, never DPI. Pure, unit-tested `src/lib/ticker-sim.ts` (12 tests) + a new measured-and-marqueeing `TabletTickerScreen` (distinct from the truncating `TabletRouteLineMock`) + the `TickerSimulator` client island; wording reuses `@shared/announcements`. Honesty guards: a ±3% "borderline" band (browser vs Android metrics) and a 4%-per-side ticker-padding inset, both erring toward warning. Scoped to sideways overflow only — the `PassengerTierMath` height degradation is deliberately out (captioned). `pds-dashboard` `bfbfc94`, pushed + prod-deployed; the boundary matched the real tablet with no calibration change.

### Phase 4 — Public face & documentation — **OVERTAKEN BY CLOSURE** (landing page + hardware guide shipped; legal/pitch/Play Store/pricing cancelled)

- ~~**Public landing page — inside the existing Next.js app as a pre-login front page**~~ — **DONE 2026-07-23 (ninth session), live on `pds-dashboard.com`** (ledger #41; session file `2026-07-23-public-landing-page.md`). Shipped as the `(marketing)` route group with the live tablet-demo hero — the "AI visuals / video" idea was superseded by the owner-picked live product demo. Residue riding on later Phase 4 items: the pricing figure (placeholder until the pricing model lands), contact email + footer © name (owner-fill), and the deferred OG image.
- ~~**Hardware recommendation document**~~ — **DONE 2026-07-25 as the public `/hardware` page** (tenth session): page-over-PDF (owner-decided — recommendations rot; the page is also the sharpest BYO pitch; print CSS gives the dated snapshot), minimum spec derived from what the app actually requires, consumer-grade recommended over rugged, ~£240-per-vehicle cost strip, cross-linked from nav/BYO section/FAQ. The test-unit/mount purchase and large-supplier identification were closure-cancelled.
- **Legal:** Claude drafts privacy documentation and signup terms & conditions (owner reviews); the legal-entity question is the owner's own decision/action. Fold in a quick **UK IPO trademark screen** for the pinned-screen mark (ledger #35) — standard pre-launch hygiene; risk assessed low (2026-07-18, third session).
- **Pitch pack:** the document set needed to pitch the product "like you would to an investor."
- **Play Store:** research the approval + publication path for the tablet app.
- **Pricing model:** work out what this will cost, with attention to small vs large operators (likely tiers). Feeds Phase 5.

### Phase 5 — Payment gate + billing (former item 7; deliberately late) — **CANCELLED 2026-07-26, never started** (the designed-but-unbuilt pricing/trial/Stripe/Ltd plan is recorded in the tenth session file)

The last substantive build item before real launch, scoped as one piece: the **enforcement half** (a subscription/operator-status dimension enforced at the existing middleware + RLS chokepoints — the three-state machinery was retained for exactly this, ledger #28) and the **commercial half** (payment provider — Stripe? — and payment tiers on the website). Open signup's "anyone can sign up" exposure stays accepted until this lands (ledger #28). Low urgency while the product is unannounced; a hard prerequisite for any paid/public launch.

### Phase 6 — Pilot readiness, the pilot, and the launch gate — **CANCELLED 2026-07-26** (the pilot operator's withdrawal was the closure trigger)

Calendar-driven by the bus company; slides earlier when their timeline firms up (the pilot does not need Phases 4–5 — the pilot operator won't sit behind the payment gate).
- **Email deliverability package** (promoted from Defer Without Guilt — do before the pilot operator signs up): DMARC `rua=` aggregate reporting, enriched Supabase auth-email template, measurement (Postmaster Tools + mail-tester), optional hardening. The auth foundation is already correct and passes DMARC — do **not** "fix" DKIM/SPF (audit detail retained in the Defer entry below).
- **Production test-data cleanup** before the pilot operator onboards — `prodDebug` testing has written device/auth/journey rows into prod (see Operational Anchors).
- **Kiosk provisioning (Device Owner / MDM)** — the deployment-time lockdown that suppresses OEM overlays uniformly (Lenovo pen button, the ZUI freeform sidebar, notification shade, nav gestures). The current Level-1 kiosk (screen-pin + default launcher) doesn't; per-device `settings put` hacks (done on the dev tablet — see Bank-and-Assess) don't scale. Provision the pilot tablet(s) as Device Owner (zero-touch / QR enrolment or a light MDM). Owner-decided 2026-07-20 not to build app-side overlay-blocking — it's a provisioning concern, not app code.
- **The pilot itself**, driven by owner-led iterative testing.
- **One final deep cybersecurity run** over the whole system — the pre-launch / pre-marketing gate.

---

## Defer Without Guilt (post-pilot)

- ~~**Repeat-last-announcement button** (FR-AT-41)~~ — **CLOSED 2026-07-18, superseded by a built feature**: the owner redesigned it into the **trigger-announcement menu** (any announcement on demand with literal-text previews, ledger #34), which shipped with the driver-panel grid rework. No retained-last holder was ever needed. Do not re-open.
- **Server-alignment hardening — triaged 2026-07-10, all re-banked as not-pilot-visible** (the old "~9 items" line, re-derived by a spec-vs-implementation diff since the enumerated list didn't survive the migration; full detail in `sessions/2026-07-10-bank-and-assess-and-server-alignment.md`). Each verdict, with the reason it can wait:
  - *pair-device rate limit* (spec: 10/10min + exponential backoff + `Retry-After` + double-weight invalid codes; impl: 10/15min plain 429, silently skipped when the client IP is unresolvable, fail-open on RPC error) — pairing happens once, by the owner; the tablet treats 429 as transient; multi-operator-scale defence.
  - *recover-device limits* (spec: 5/hr per android_id + 15-min lockout + per-IP 20/hr; impl: 20/15min per android_id only, no lockout) — **impl is deliberately MORE permissive than spec**; tightening toward the spec'd lockout could brick a flaky pilot tablet's recovery. Leave as-is.
  - *auto-deregister* (spec: compound 60-day-silent AND ≥5 terminal recover failures; impl: single 60-day-silent condition — harsher than spec) — 60 days offline is beyond the pilot horizon; recoverable by re-pairing.
  - *`devices_update_by_device` WITH CHECK tightening* (device JWT can also write `device_secret_hash`, `user_id`, `recover_failure_count`, `registered_at`) — exploitable only with a compromised device JWT; the fix is a **production migration** for zero pilot exposure. Deliberate post-pilot task.
  - *`retry-admin-notification` Edge Function* (spec §3.1, never built) — owner onboards the pilot company personally and approves manually.
  - *`device_pairing_codes` hourly cleanup* (never built; rows accumulate) — pair-device rejects expired/used codes regardless; cosmetic.
  - *FCM payload naming* (spec `route-sync` vs deployed `route_audio_ready`) — **closed as accepted divergence**: both ends agree with each other; documented in the Android CLAUDE.md.
  - *(`devices.active_route_id` was the one pilot-visible item — promoted and **built**, `684879d`.)*
- **ANDROID-3 deep self-heal** — permanent Keystore key loss → recreate keyset → pairing screen instead of a crash-loop. The transient class is covered by the 2026-07-10 retry (`6ba13cf`) and the permanent restored-keyset vector died with B1 `allowBackup=false`; build the self-heal only if the pilot ever surfaces a recurrence.
- **The frozen-doc "divergences"** — under the "docs are evidence, not authority" framing, divergence between the frozen planning docs and the current implementation is *expected and fine*. The old re-planning campaign is **dropped**. (New instance this session: the six-DSN Sentry plan was superseded by one-project-per-surface + environment tag — recorded in the dashboard CLAUDE.md/README, ledger #20.)
- **@sentry/deno v10 upgrade** — pinned at 8.55.2 (the Supabase-docs-validated major); bumping is a deliberate toolchain task, not feature collateral.
- **Email deliverability optimisation for auth emails (signup confirmation / password reset)** — **promoted into roadmap Phase 6** (2026-07-17 — do before the pilot operator signs up); kept here for the audit detail. Surfaced by a read-only DNS audit 2026-07-12 (eighth session). The **authentication foundation is already solid and passes DMARC** — a correct, standard Resend setup: DKIM key at `resend._domainkey.pds-dashboard.com` (`d=` root, aligns *strict* with `From: noreply@pds-dashboard.com`), SPF `v=spf1 include:amazonses.com ~all` on the `send.` subdomain (the return-path, aligns *relaxed*), and the `feedback-smtp.eu-west-1.amazonses.com` bounce MX all live and correct. So junk-foldering is **NOT** an auth failure — do not "fix" the authentication. By elimination the real causes are **new-domain reputation** (`pds-dashboard.com` is days old, no sending history — heals with time, not a setting) and the **sparse Supabase default template** (one line + one link, HTML-only — reads phishy to filters). The deferred optimisation, as one package: (1) add DMARC **`rua=` aggregate reporting** (currently bare `p=none;` → zero visibility; highest-value single add); (2) **enrich the Supabase auth email template** (plain-text part + a context sentence + a footer — the one piece Claude can edit); (3) stand up **measurement** (Google Postmaster Tools + a mail-tester.com score); (4) *optional hardening* — restrictive apex SPF (`v=spf1 -all`), DKIM 2048-bit regen (Resend-side; current key is 1024-bit but passes), and `p=none → p=quarantine` once `rua` reports show clean alignment. **Not pilot-blocking, but worth doing before the pilot operator is signing up** — a verification email in their junk is a bad first impression. Full audit detail in `sessions/2026-07-12-admin-pin-4-digit-and-email-deliverability-audit.md`.

## Bank and Assess (narrow, non-blocking)

**The "small nits banked (fix opportunistically)" list is fully cleared as of 2026-07-12 (sixth session — see `sessions/2026-07-12-review-nits-slice-and-unit-types.md`):** the CalibrationScreen-scroll nit, the whole three-lens review-findings set (a–e), the "8→9 Edge Functions" doc-nit, and the e7 `Mm`/`Px`/`Ppmm` unit types were all shipped, pushed, and owner-glass-verified. Only **accepted "do-not-fix" postures** and optional deferrals remain below — these stay in the living doc on purpose (their function is to stop a future session re-opening settled behaviour):

- **Detection edge cases — dispositioned 2026-07-17 (second session); the two below are accepted DO-NOT-FIX postures.** Of the four banked cases, (1) overlapping geofences and (4) the authoring note were **built** as the route-builder geofence warnings (`e746638`): severe = a stop centre inside a consecutive neighbour's radius (the geometry that jams the departure gate / fires the look-ahead early); **mild ring overlap deliberately unflagged** — traced as benign under the announce-on-departure model, and flagging it would bury the real warning. The remaining two stay accepted by design: **(2) single-fix multi-stop skip** — the trigger is a GPS *outage*, not speed (at the 2 s cadence a 30 m/s bus moves 60 m between fixes, inside any ≥80 m radius); an outage already fires the 60 s GPS-lost warning so the driver is alerted exactly when the risk exists, manual advance is the designed recovery, and widening Rule 5's N/N+1 window would trade directly against the ledger-#31 hysteresis hardening. **(3) duplicate-coordinate stops in the start scan** — running detection is immune by construction (N/N+1 only); the exposure is only the one-time `findStartCandidate` scan, requires a route that crosses its own path AND a mid-route journey start at the shared point (return journeys are separate generated routes, so out-and-back duplicates don't exist within one route), the driver confirm/decline prompt + manual advance recover, and the new dashboard duplicate-location warning now flags it at authoring time anyway. **Do not re-open either without new pilot evidence.**
- ~~**Pending glass check (small):** the `e20fe6d` TopAppBar theme-toggle fix + the `c80ea15` pinned-screen launcher icon~~ — **BOTH owner-confirmed on glass 2026-07-18 and discharged.** No launcher-scale tweak was needed. No glass debt remains on either.
- **NaPTAN search cannot reach a stop whose name is a common token pair — accepted DO-NOT-FIX (found 2026-07-29).** `searchNaptan` orders by `stop_name` and caps at 20 (`src/lib/actions/naptan.ts`), so the stop named exactly **"Victoria Station"** (`4900VICTRIC0`) is unreachable: its only search tokens are `victoria` and `station`, every query matching it matches all 88 such stops, and it always sorts 69th. Searching a stop's exact name does not return that stop. A real UX defect, but the product is closed and it is not in the shot list, so it stays unfixed; the demo route uses `9100VICTRIC` "London Victoria Rail Station" instead, which suits a rail-replacement demo anyway. If this is ever revisited, the fix is ranking by `ts_rank` (which the dashboard `CLAUDE.md` already *claims* happens) or exact-name boosting — **not** raising the limit.
- **Dev-tablet local state (2026-07-18, extended 2026-07-20):** two ZUI overlays are disabled system-wide on the dev tablet — the Lenovo **pen floating button** (`settings put secure pen_set_show_button 0`; revert with `… 1`) and the **ZUI freeform sidebar** (the left-edge swipe-out app dock: `settings put system enable_zuifreeformbar 0` + `enable_temp_zuifreeformbar 0`; revert with `… 1`). Both owner-decided keep-off; **no app machinery** — an OEM edge overlay isn't app-suppressible without Device Owner, and a deployed kiosk is always-foreground so per-app ≡ system-wide. The uniform fleet fix (all such overlays at once) is Device Owner/MDM kiosk provisioning — see Phase 6. Also observed twice: ZUI aggressively OS-kills the backgrounded `prodDebug` app (no crash) — harmless for kiosk use, explains the app "not running" after idling on the launcher.
- ~~**Staging's 4 live routes still carry the OLD audio wording**~~ — **CLEARED 2026-07-20:** owner re-saved all four; confirmed re-rendered (`audio_version` moved to today's epoch, status `ok`). Only staging's fifth route, **"C2 Test Route V2" (`RR1`)**, still carries pre-designation audio (June-10 version) — not refreshed; harmless, re-save if ever needed for staging testing.
- ~~**No force-re-render for a HEALTHY route**~~ — **BUILT 2026-07-20 (ledger #40),** deployed to prod and glass+backend-verified. `triggerReRender` now runs on `ok`/`failed` (only `pending` blocked) and a fleet-wide `bulkReRender` exists; MCP-read-only (ledger #22) is no longer a blocker because the re-render is a normal authenticated dashboard action. Do not re-open.
- ~~**The design language has no interaction-affordance section at all (new 2026-07-21).**~~ — **DONE 2026-07-23.** Added a **"Interaction & affordance"** section capturing the *principle* — clickable things look clickable (the central `globals.css` cursor rule as one worked example), plus the case the CSS structurally can't reach (a clickable `<div>` with no `<button>`/`role` → a real button/link, or `role="button"` + `tabIndex` + a key handler), focus-visible never stripped, and disabled-never-advertises-a-click. Synced across all three ledger-#27 copies: the skill `SKILL.md`, `living/DESIGN-LANGUAGE.md` (bodies verified byte-identical), and a new `interaction.html` Claude Design card (default/hover/focus/pressed/disabled, both themes; project now 14 cards, stale "12 cards" count corrected in both docs). No Rule 16/20 change — the principle generalises those rules. Not a rider on a CSS commit, as scoped.
- **Dashboard `CLAUDE.md` refresh banked (2026-07-23, ninth session; grew 2026-07-26).** The doc now trails two sessions of structure: the `(marketing)` route group, `/` public (`DASHBOARD_PATHS` no longer contains `'/'`), the session-aware public header, the `TabletDemo` third terminating-wording copy — plus, from the tenth session, the `/hardware` page, marketing print styles, `MarketingLogo`, the nextjs-toploader dependency, and the shared London RR1 example route (demo + `/guide` SAMPLE + simulator DEFAULTS, cross-referenced). Stamp still cites `7291ae0`. Fold into the portfolio conversion's docs pass — a docs commit rides the normal human review-and-push gate.
- **Landing-page small follow-ups (2026-07-23, ninth session; none blocking):** owner-fill placeholders on the live page (pricing figure — arrives with the Phase 4 pricing model; contact email for the final CTA; footer © legal name); the deferred OG image (ledger #41); and two unexercised checks — the logged-in "Open dashboard" header state on the live site, and `prefers-reduced-motion` (static demo frame; needs OS-level emulation).
- **Workspace-root `.playwright-mcp/` re-accumulates (noted 2026-07-21; swept again at the 2026-07-23 ninth-session close-out after the landing-page Playwright pass, along with stray screenshot files at the workspace root).** Playwright MCP verification runs drop console logs and page snapshots into a `.playwright-mcp/` directory at the workspace root; 24 files had built up since 11 July before being cleared. The eighth-session close-out found it absent (that session's verification was Playwright-free); the ninth session's landing-page Playwright pass re-created it and it was swept at close-out. The workspace root is not a git repo, so no `.gitignore` covers it and nothing tracks it. Same CWD-hygiene family as the supabase-CLI/vitest scratch dirs — worth a sweep at close-out, or a one-line ignore if it becomes annoying. (Also noted: `rm -rf` is blocked by a permission guard on this machine; PowerShell `Remove-Item -Recurse -Force` is the working equivalent — a denial there is the guard, not an owner refusal.)
- **Reported, not fixed — dashboard Rule 10's per-stop differential re-render claim was never true (2026-07-18).** Rule 10 stated per-stop files were skipped "keyed on the stop name"; the deployed `audio-render-worker` has no per-stop hash and no copy path, and re-synthesises **every** `stop_{N}.wav` on **every** job. Only the route announcement is hash-gated. Per the workspace rule the **documentation was corrected and the code deliberately left alone** — the practical consequence is that a re-render costs full TTS for every stop, which the cost estimate now states honestly.
- **Cross-repo announcement-wording drift is an accepted residual risk (2026-07-18).** Nothing mechanically binds the Kotlin `strings.xml` wording to `pds-dashboard/supabase/functions/_shared/announcements.ts`; the failure mode is a tablet that SHOWS one thing and SAYS another on a regulated surface. Mitigated only by bidirectional code comments, the two mirrored test tables, and hardware verification. Closing it properly would need Robolectric on the Android side (a deliberate toolchain task, not feature collateral) or a generated-string pipeline — neither is worth it at this scale, but the risk is named rather than pretended away.
- **Ticker-simulator constants are a new accepted cross-repo copy (2026-07-23).** `src/lib/ticker-sim.ts` copies the tablet's ticker constants (`MIN_PASSENGER_TEXT_MM`, `COMPLIANCE_MARGIN_MM`, `TICKER_SPEED_MM_PER_SEC`, `TICKER_INITIAL_DELAY_MS`, `TICKER_GAP_MM`) and `HERO_CANDIDATE_MM` from `pds-android` (`util/PhysicalTextMath.kt` + `presentation/journey/JourneyScreen.kt`), the tablet being the source of truth. Same posture as the fixed-announcement-string copy in `guide/page.tsx`: nothing binds them mechanically, mitigated only by a source-of-truth citation in the file header — if a constant changes on the tablet, change it here. The blast radius is small (this is an authoring *estimate*, not a regulated surface — the tablet remains the compliance authority), and the simulator's `TICKER_SIDE_INSET_FRACTION` (0.04) + ±3% verdict tolerance are the calibration levers if a future tablet cross-check ever disagrees (the 2026-07-23 cross-check matched with no change).
- Fleet-view "active route" cell is request-time rendered — a tablet-side change needs a page refresh. Auto-refresh is a small future dashboard feature, only if the pilot wants it; the `RenderStatusPoller` (`routes/_components/`, added 2026-07-20 — `router.refresh()` on a timer while a condition holds) is a ready pattern to reuse.
- **Deferred-by-choice from the 2026-07-12 go-live session** (all optional, none blocking): **staging SMTP** (only if email testing on staging/local previews is ever wanted; prod is done); **email-template Booking Hall rebrand** (the one piece Claude can edit — Supabase → Emails → Templates — declined for now, owner likes the default bodies); **Speed Insights** (skipped; enable later if dashboard perf ever matters); a **GitHub Actions CI gate + branch→preview→prod flow** (owner prefers the current push-`main`-to-prod simplicity).
- **From the 2026-07-12 admin-navigation session:** owner glass pass of `29cc3c3` **confirmed on the Lenovo 2026-07-12** (calibration gesture-back/arrow/Done each → admin menu without PIN; no toggle/gear floating on the calibration screen) — discharged. (The `CalibrationScreen` SliderStep off-screen-button nit was **fixed 2026-07-12**, `cf26973` — the step now scrolls.)
- **From the 2026-07-11 tickers/tiers session (discharged 2026-07-12):** the formal 22 mm ruler check on the tickers *mid-scroll* and the Lockito diversion pass at Tier A were **both owner-confirmed on glass 2026-07-12** — no ticker/tier glass debt remains. The software half had already been discharged 2026-07-12 by an emulator screenshot sweep (see `sessions/2026-07-12-skills-design-sync-and-tier-sweep.md`): all four tier states photographed and correct (A → B1 → B2 → C, both themes), the 72 dp compact tube band eyeballed clean, and the Tier-C Reg 10(1) "Not stopping at:" carrier captured mid-marquee at the 22 mm floor. Tier B/C still never seen on *real* small hardware (none deployed — fine). The banner's operator name populates on the first sync of the new build (wordmark fallback until then).
- A `journey_end` sync trigger landing mid-sync drops as `AlreadyRunning` → stale fleet cell for ≤30 min (periodic self-corrects) — accepted by design (drop-not-queue is load-bearing); revisit only if operationally annoying.
- FCM-secret-missing in the render worker is deliberately log-only (unchanged posture).
- Dashboard beforeSend scrubbing deliberately out of scope (Next SDK attaches no request bodies by default; unchanged posture).

---

## Decision Ledger (standing decisions in force)

These are the decisions that currently shape how work is done on this codebase. Full historical decision detail is in `PROJECT-HISTORY.md` and the code itself; this is the curated set still load-bearing.

**Compliance rules (inviolable; every announcement-touching change inherits them):**
1. **Co-equal visual** — the screen flash and text overlay for a regulated announcement fire regardless of `audio_enabled`; a silent tablet still shows them. Any audio-mute must gate *only* the audio, never the visual.
2. **Asymmetric undroppable locking** — a regulated announcement interrupts a non-regulated one; a non-regulated one never interrupts; regulated-vs-regulated drops the later. A legally-required announcement is never silently lost.
3. **Event-index vs approached-index discipline** — the announced/passed stop and the approached stop are distinct indices; audio and boundary logic key on *approached*, event logging on *announced*. Terminus emits null, never clamp-to-last.
4. **≥22mm physical text** — passenger text is sized by physical measurement (calibrated) and never truncates below the floor; it grows line count instead.

**Working disciplines:**
5. **Deployed-shape audit before touching any deployed surface** — audit the actual deployed function/RPC/schema shape, don't trust the spec. Has caught real defects repeatedly (latest: 2026-07-09 — `generate-pairing-code`/`enqueue-render-job` are `verify_jwt: true` unlike the other six; a blanket `--no-verify-jwt` redeploy would have silently changed their auth posture).
6. **Plan-mode-first** — for any build, produce a plan and get it reviewed before editing; plan-mode review is a real gate, not a rubber stamp.
7. **One-prompt-one-commit; commit via message-file** (not a here-string — avoids stray `@`). Compliance features are glass-verified on the real Lenovo tablet before commit.
8. **Docs are evidence, not authority** — the frozen planning docs record what was designed/built; current implementation + judgment are authoritative where they differ. Divergence is expected.

**As-built design record for the driver-controls / audio-output slice (built 2026-07-08; #9–15 were the pre-build decisions, all honoured — see also #17–19 for the plan-review refinements):**
9. **Whole slice stays schema-free.** No Room migration. Emergency Mute state is an **in-memory journey-scoped singleton** (evaporates on process death — accepted; avoids a schema bump during the workflow migration); it still logs `MUTE_ENGAGED`/`MUTE_RELEASED` to `journey_events` (additive, no migration).
10. ~~**Volume slider** binds to the live accessibility stream, clamped to `floor..100%` via a shared clamp helper; **hardware volume keys** use an explicit key override (not `volumeControlStream`) routed through the same clamp, so the floor is honest at key-time. The admin-menu volume-floor setting stays the PIN-owned authority; the driver slider moves live volume within it.~~ _SUPERSEDED 2026-07-18 by ledger #33: the on-device diagnosis proved this mechanism could never work (apps cannot write STREAM_ACCESSIBILITY). The PIN-owned floor-setting authority carries forward; the slider and the key-time clamp are gone._
11. **Emergency Mute** gates inside the announcement player *only* (alongside the existing `audio_enabled` early-return, before the volume-floor raise) — this is architecturally identical to `audio_enabled = false`, which already proves the visual path is untouched (ledger #1). Never gate mute upstream at the announcement coordinator.
12. **AudioFocus** is requested per chimed announcement pair (not per-journey), abandoned in the same release path; a focus *denial never silences a regulated announcement* (request-but-play-regardless, breadcrumb the denial).
13. **Bluetooth disconnect** uses `AudioDeviceCallback` feeding a singleton shaped like the GPS-signal monitor (as built: the callback registers in `LocationService` for the journey's lifetime and feeds the `ExternalSpeakerMonitor` singleton — the same shape as GPS fixes feeding `GpsSignalMonitor`; precision recorded 2026-07-09), with `NEVER_CONNECTED` / `CONNECTED` / `DISCONNECTED` states — only `DISCONNECTED` warns. The warning reuses the two-surface amber-marker pipe (status strip + panel-header re-surface); two simultaneous markers stack vertically.
14. **Keep-alive** plays on a `LocationService` coroutine ticker (not WorkManager — its 15-min floor can't do a 10-min tick), via a **separate silent playback path that never touches the announcement mutex** (so it can never drop or be dropped by a real announcement), skipping the volume-floor and AudioFocus; gated on `audio_enabled`; keeps playing under Emergency Mute (mute is about audible output); runs unconditionally rather than gating on Bluetooth state.
15. **Manual H&R fallback** introduces an **in-memory** `manualHailRideActive` state (not persisted — its correct lifetime is "until the driver ends it or GPS recovers," and the compliance-safe failure direction on a crash is *toward* re-enabling normal announcements) that the next-stop-suppression check ORs with `segment_type` at both call sites. Manual state is authoritative while active: a GPS-derived section start/end is a no-op while it's on. The manual use case flips the state and logs the event even if the announcement itself is drop-overlapped (the state change is the load-bearing effect); the button shows brief press confirmation. Wired through the view-model via dedicated use cases, never the composable touching the announcement bus directly.

**Documentation discipline (added 2026-07-05):**
16. **Repo CLAUDE.md rule numbers are frozen.** Code comments in both repos cite the architectural rules by number ("Rule N"), so rule numbers are never reused, reordered, or repurposed — a retired rule's number stays retired, and new rules append at the end of the list. New feature areas are documented as conventions unless they carry a genuinely new inviolable constraint.

**Plan-review refinements from the driver-controls slice (added 2026-07-08; each owner-approved in plan review):**
17. **Manual H&R buttons are always-fire** (PRD FR-AT-41 verbatim, chosen over a strict transition-only toggle): every press logs the section event, fires the chimed announcement "regardless of segment boundaries", and idempotently sets/clears the manual override — a non-transition press still announces, giving one-tap recovery when GPS auto-fired a section start but missed the end. A repeated announcement from a re-press is deliberate correction behaviour, **not a bug to fix**.
18. **Focus-denial asymmetry:** a non-regulated announcement (next-stop / route-and-destination) **drops** on an AudioFocus denial (breadcrumbed); only regulated audio plays regardless (#12). The keep-alive requests no focus at all (#14).
19. **The speaker-disconnect warning is driver-dismissible** (PRD FR-AT-30 verbatim, chosen over the plain GPS-marker mirror) — a per-episode `DISMISSED` machine state, with the dismiss affordance in the panel header only; and an engaged Emergency Mute shows a third amber "Audio muted" strip marker, so a silently-muted tablet is never visually normal.

**Sentry telemetry posture (added 2026-07-09; owner-approved in plan review):**
20. **One Sentry project per surface** (`android` / `dashboard` / `edge-functions`, org `passenger-display-system`, EU region), staging vs production split by the `environment` tag — supersedes the frozen six-DSN plan (Android precedent). **Sample rates are 0 on every surface** (errors and crashes only; protects the shared 5k/month free tier). Edge environment is derived from the `SUPABASE_URL` project ref (override: `SENTRY_ENVIRONMENT`); the edge DSN is the `SENTRY_DSN_EDGE` Supabase secret (same value both projects); the dashboard DSN is `NEXT_PUBLIC_SENTRY_DSN_DASHBOARD` in Vercel. SDK pinned `npm:@sentry/deno@8.55.2`.
21. **Edge capture rules (empirically forced, regression-guarded):** the scrubber's hex-redaction threshold is **40, never 32** — Sentry's own protocol IDs are exactly 32 hex and a ≥32 rule makes ingest silently 400-reject every event; and per-event context goes through **captureException's capture-context argument, never `withScope`** — scope mutation does not isolate on the per_worker edge runtime (contexts provably leaked across captures). Quota discipline: expected outcomes (401/403/404/429) are never captured; `audio-cleanup-worker` emits one end-of-run summary event, not per-route events; dedupe integration on.

**MCP + workflow posture (added 2026-07-09, second session; owner-decided):**
22. **Two permanent read-only Supabase MCP servers** — `supabase-staging` (`febcpudiorwnsbcphcng`) and `supabase-prod` (`qopkjsihmdplmsoqrzji`), each pinned to its project, read-only enforced at the MCP server layer, permanently (no re-pointing). Production is readable, never writable, via MCP; the environment is explicit in every tool name — verify identity (`get_project_url`) before trusting an environment read. Supersedes "staging-pinned at rest + sanctioned temporary re-point". Owner-offered MCP write-mode was **declined**: all writes (migrations, function deploys, MCP write-mode changes) remain human-gated.
23. **Session close-out commits and pushes `pds-planning` automatically** (memory ritual step 3, workspace `CLAUDE.md`): Claude stages the session file + STATE.md update, commits via message file, and pushes `origin main` — a deliberate exception to the human-push gate, because the planning repo is documentation/memory only (nothing deploys from it). Code-repo pushes stay human-gated.

**Driver-surface occlusion posture (added 2026-07-10, fifth session; owner-approved and glass-verified):**
24. **A driver-opened control surface may temporarily cover the hero/passenger text.** The driver panel (expanded to wrap its control stack, `e34414f`/`67f8412`), `JumpToStopOverlay`, and `StopSelectorOverlay` share one posture: Reg 7/9 precedence is preserved by construction — the surface composes BELOW the regulated flash/overlay, collapse-on-fire closes it the moment a regulated announcement lands, it is driver-opened only, and the panel's 30 s auto-dismiss returns the passenger view. Supersedes the driver panel's original "non-occlusion by construction" height cap. Glass-verified: a regulated H&R announcement took the screen over the open panel.

**Booking Hall design system (added 2026-07-11; owner-designed, glass-verified). _VISUAL half superseded by #32 (Steelglass) 2026-07-17; the mechanisms — driver-controlled theme, palette-only colour flow, semantic amber, letterspaced-caps-off-passenger, coordinator-owned flash timing, cap-height measurement — remain in force under the new identity:_**
25. **"Booking Hall" is the product's visual identity, and the tablet theme is a driver-controlled setting.** Gelasio serif; parchment light / Wheat dark; ticket-edge, stamp, and serial-Nº idioms; one-tap half-circle toggles (dashboard cookie, light default; tablet `DisplayThemeStore`, dark default — never system/OS-derived, kiosk-uniform per setting). Load-bearing sub-rules, enforced by repo Rules 16 (dashboard) and 20 (Android) plus `PassengerPaletteTest`: passenger colors flow only through `LocalPassengerPalette` (the pre-redesign "always dark, not theme-switchable" posture is superseded); **amber warning roles stay amber in both themes**; letterspaced caps are dashboard/driver-side only (Reg 14(5)(a) — passenger surfaces stay mixed case); the announcement flash/overlay is the palette inversion with **colors only** palette-driven — `RegulatedAnnouncementCoordinator` timing is untouchable from design work; the 22 mm cap height is measured from the bundled Gelasio faces (minimum across weights, measured face = rendered face). Design authority: repo rules first, then `living/DESIGN-LANGUAGE.md` / the workspace `pds-design-language` skill (manually-synced copies).

**Minimum-text ticker + tier posture (added 2026-07-11, second session; owner-approved through plan review + four glass rounds). _The ticker/tier ENGINE and every compliance behaviour below remain fully in force; only the entry's Booking Hall visual vocabulary is superseded by #32:_**
26. **The persistent regulated lines are single-line horizontal tickers; layout degrades, text never does.** Route name + final destination (one line — the Reg 7(1)(b) fix) and next stop render through `PassengerTickerLine`: **static when they fit** (fully-visible text is the conservative reading of "displayed"; motion is strictly the overflow fallback), physical-speed marquee when they don't (`TICKER_SPEED_MM_PER_SEC` — deliberately separate from the overlay's glass-verified scroll constant), never wrapped/truncated/below 22 mm. The regulated overlay stays a vertical teleprompter — unification is at the engine level, not the visual level. `PassengerTierMath` degrades **layout only** by computed fit (band → caption → combined ticker; the plan has no font-size output by design); **Tier C's combined ticker is the sole Reg 10(1) carrier** (appends "Not stopping at: …" — the tube map is gone at that tier) and knowingly softens continuous route-name display (owner-accepted; phones are non-deployable under Reg 14(2) anyway). Also in force from this session: manual sync is deliberately **not** PIN-gated (idempotent single-flight ⟳ on the route-list strip; the admin force-sync is deleted); the theme toggle + admin gear live in the app-wide top-end cluster (obvious, scheme-drawn — the "deliberately faint handle" posture is superseded); the journey banner band hosts that cluster so the tickers run full width; the pop-up cancel-X idea was **rejected** on compliance grounds (a dismiss on the regulated overlay is a kill switch for legally-required information — collapse-on-fire stays inviolate).

**Design-system sync + third-party-skill posture (added 2026-07-12; owner-mandated):**
27. **Any design/colour/idiom change updates all three design-system copies in the same session** — the workspace `pds-design-language` skill, `living/DESIGN-LANGUAGE.md` (word-for-word identical body), and the "PDS Booking Hall" Claude Design project cards (via DesignSync; the procedure is written at the bottom of both documents). A design change is not done until all three match; repo Rules 16/20 outrank all three copies. **Third-party skills** (chrisbanes Compose/Kotlin pack, official Supabase, Anthropic document skills) are installed at workspace `.claude/skills/` with deliberate exclusions (no autonomous PR/issue workflows — they conflict with the human-gated push posture) and local override notes (the `supabase` skill's schema-change workflow is overridden: MCP stays read-only, writes human-gated). Workspace/repo rules always outrank an installed skill; provenance and re-sync rules live in `.claude/skills/README.md`.

**Open signup + payment-gate posture (added 2026-07-12, fifth session; owner-decided):**
28. **New operators start `active`, not `pending`** (migration 025 — `handle_new_user()` writes the literal `'active'`). Manual approval is retired. The full three-state status machinery (middleware `/pending` + `/suspended` gates, RLS `operator_status = 'active'` mutation gate, enum CHECK) is **deliberately retained** — `pending`/`suspended` stay reachable administratively and re-adding an approval step is a one-line migration. The accepted "anyone can sign up and use it" exposure is temporary and explicitly bounded by the **planned payment gate** (What's Left #7), which becomes the real access control and is expected to extend the same middleware + RLS chokepoints. Only the trigger literal changed; existing rows were not migrated.

**Self-service account deletion (added 2026-07-12, fifth session; owner-decided):**
29. **An operator can delete their own account, hard-wiping all their data.** The destructive work lives in the `delete-account` Edge Function (Rule 5 — service-role admin API, never a Server Action); the dashboard action forwards a **re-entered password** as the confirmation. Deletion order is load-bearing: wipe the operator's `route-audio` Storage prefix + delete device auth users FIRST, delete the operator's auth user LAST (its `ON DELETE CASCADE` tears down every Postgres row), so a mid-way failure leaves a still-deletable operator rather than orphans. **No admin exception** — self-service delete will wipe the owner's own admin account too (accepted). The delete-with-data path (device users + Storage) is unexercised on glass by owner choice; the empty-account path is prod-verified.

**Driver-panel button styling (added 2026-07-12, fifth session; owner glass-decided). _The solid-fill principle and both semantic exceptions carry forward under #32; the Booking Hall colour values are superseded:_**
30. **Driver-panel control buttons are flat SOLID fills, no border; amber is never a button accent.** Every `DriverControlOverlay` button is ink fill + ground text (the `onBackground`-on-`background` inversion, "opposite of the background"), except End Journey (`ephemera` crimson, the one distinct terminal action) and Emergency-Mute-engaged (`warning` amber, the muted-state indicator — ledger #19). `warning` amber is otherwise confined to the GPS-lost / speaker-disconnect markers. A dashed "stamp" border was tried and **glass-rejected** (tacky; bled over the fills) — there is no border/ghost/outline treatment on the driver panel (the ticket-edge ghost-button idiom is dashboard-side). No new palette roles; refines Rule 20 + ledger #25. Synced across all three design-system copies (ledger #27).

**Announce-on-departure model (added 2026-07-16; owner-decided through plan review + glass testing):**
31. **The next-stop announcement fires on DEPARTURE from the previous stop, not on arrival at it.** GPS stop detection is arm-only on arrival (entering a stop's radius sets a tagged departure gate, announces nothing, holds the displayed next stop); "Next stop: X" + the `current_stop_index` advance fire when the bus **departs** the previous stop (exits its radius). So the hero + tube-map hold "Next stop: X" through the whole approach and while stopped at X, flipping to X+1 only on departure (audio + visual in lockstep — co-equal visual, ledger #1). This is the Reg 9 "on-departure" model (gov.uk blesses both on-departure and on-approach; matches the frozen Compliance-Mapping-Matrix Reg 9(2)). Load-bearing sub-decisions: **arrival is arm-only**; the **terminus** fires completion on the arrival edge (the bus never departs it) via a **boolean** `gpsTerminusArrival` flag — deliberately a boolean, NOT a third index, so Rule 18's announced-vs-approached two-index framing is untouched; the **look-ahead advances ONE stop** (a late catch-up), not two; journey start primes `current_stop_index = 1` + an `AnnounceOnDepart(stop 0)` gate so the **origin is never announced as its own next stop**. **Informational-audio queue (Rule 19 refinement):** the first "Next stop" fires ~2 s after start and collided with the route-and-destination announcement → non-regulated announcements now use a **single-slot latest-wins queue** (park behind another informational one and play when it finishes; still drop behind a regulated one; a regulated announcement wipes any queued informational). Rule 19's inviolable regulated guarantees are untouched. Rejected alternatives: the two-cue "Next stop X" + "This is X" idea (Reg 9 is one requirement, not two), and the on-approach variant. **Robustness hardening (added 2026-07-17, glass-verified; the two previously-open items are now RESOLVED).** (a) **Geofence temporal hysteresis** (`46697b2`) — an actionable transition (arrive / look-ahead / depart) is acted on only after it persists `CONFIRM_FIXES=3` consecutive qualifying fixes (`MAX_FIX_GAP_MILLIS=6s` continuity guard); `confirmDecision` wraps the *untouched* `decideStopDetection`, the departure-gate state IS the accrual memory, and a mid-accrual NoOp is *refuting* when accurate (reset) but *held* when coarse (the compliance-safe direction — a jittery fix never wipes a legit accrual, so it can only bounded-delay, never drop; termination is backstopped by the stationary auto-timeout). `K=1` disables it byte-for-byte. Kills the looping-route early look-ahead AND the symmetric single-fix jitter-departure. (b) **H&R timing is compliant and UNCHANGED** — START at the last scheduled stop before the section (Reg 11(2)(a) mandates "at the last scheduled stopping place prior"; moving it to the first H&R stop would BREAK compliance), END on departing the last H&R stop; only the START *wording* moved "now entering"→"approaching" (`bc70f73`, Reg 11(1) forewarning). (c) **Coarse-GPS "Weak GPS" warning** (`6d26f78`) — a per-fix detector (no watchdog) warns the driver when fixes arrive but are too coarse to detect the current stop(s) for `COARSE_THRESHOLD_MILLIS=45s`, the silent-journey gap GPS-lost misses (it keys on fix *absence*); the pure `isStarvedFix` single-sources `passesAccuracyGate` and mirrors the detector's armed/disarmed gate branches (correct in the origin-priming window); non-dismissible driver-only amber, hidden while "No GPS" shows, off the announcement path. All three thresholds are glass-tunable and confirmed appropriate this session.

**Steelglass design system (added 2026-07-17, fourth session; owner-designed through three artifact rounds, glass-verified):**
32. **"Steelglass" is the product's visual identity** — strictly neutral monochrome, machined glass; supersedes the visual halves of #25/#26/#30 (their mechanisms all carry forward). The load-bearing decisions:
    - **The pair:** dark "D2 near-black" (`#0A0A0A → #151515` canvas, `#EAEAEA` ink) is the DEFAULT on **both** surfaces — the dashboard flipped from light-default (cookie-absent → dark; explicit `light` cookie opts out); light is "L1 pure-white" (`#FFFFFF → #F6F6F6`, `#1C1C1C` ink). The driver/cookie theme mechanics are unchanged.
    - **Surfaces:** a 160° canvas gradient with translucent glass panels (hairline border + inset top-highlight; backdrop blur web-only). Dashboard: body gradient + `glass-panel`/`chip` utilities (replacing `ticket-edge`/`stamp`), translucent `--card`. Android: the `ui/theme/Steelglass.kt` helpers own the canvas geometry and panel treatment (canvas painted once at MainActivity's root; **no blur on Android by design** — no dependency); driver overlays stay near-opaque `panelSurface`; Material dialogs stay opaque.
    - **Colour is intent-only:** ok green, semantic warning amber (unchanged rule; light value `#B45309` carries a recorded watch-item — 1° above the amber band's edge at 4.65:1, tune hue UP only), failure red, and `ephemera` re-scoped from decorative red to the **terminal crimson** (End Journey only — the serial-Nº ornament is retired; real identifiers render in the mono data voice instead).
    - **Type:** Inter (static per-weight TTFs on Android — never the variable font, the 22 mm cap-height measurement takes the minimum across bundled weights) + JetBrains Mono for data. Gelasio retired. The cap-height machinery re-measured Inter automatically; 22 mm floor owner-verified on glass.
    - **Palette contract:** the Android palette's `background` role is the gradient's WORST-contrast end (`backgroundDeep` is the other end, guarded by a new `PassengerPaletteTest` assertion), so every contrast judgement holds at every gradient point. The palette `accent` deviates from the muted greys (`#C9C9C9`/`#5C5C5C`) because the spec's muted values fail the ≥4.5:1 accent guard on the light canvas.
    - Identity resources moved with it: ~~Steelglass favicon + Android launcher icon (ticker-bar-in-ring mark, replacing the stock template robot)~~ _(mark superseded 2026-07-18 by ledger #35 — the pinned-screen logo)_, dark `windowBackground` cold start, neutral notification accent.
    - Reference: signed-off full mockups + token spec in the "PDS Steelglass — Full Mockups" artifact (`241409b8-…`); Rule 16/20 are the binding statements.

**FR-AT-44 volume posture (added 2026-07-18; owner-decided after on-device diagnosis — supersedes #10):**
33. **Volume keys are native Android; the floor lives solely in the announcement-time raise on STREAM_MUSIC.** The diagnosis (PDS.VolDiag + `dumpsys audio`, on the Lenovo): Android **silently drops app writes to STREAM_ACCESSIBILITY** (only enabled accessibility services may write it) — so the PRD's slider/key-clamp machinery never actually worked — and the platform **aliases the a11y stream to STREAM_MUSIC** (index-rescaled): announcement loudness IS media volume. The posture: no key interception, no slider (two recorded FR-AT-44 divergences — don't rebuild either from the PRD); `AnnouncementPlayer.applyVolumeFloor()` raises **STREAM_MUSIC** to the PIN-owned floor (default 30%, admin-menu setting still the authority) before every announcement in both playback paths — the sole, now-real Reg 13 floor enforcement; the driver may sit below the floor between announcements by design. Emergency Mute and `audio_enabled = false` still gate before the raise (a muted tablet's stream is never forced up). **Watch-item: kiosk units must never run accessibility services** — an enabled a11y service makes a11y volume independent and silently decouples the floor. Playback attributes stay USAGE_ASSISTANCE_ACCESSIBILITY; never retarget the raise back to the a11y stream.

**Driver-panel grid + trigger-announcement menu (added 2026-07-18; owner-designed, glass-verified):**
34. **The driver panel is a fixed four-row grid, and the trigger-announcement menu supersedes FR-AT-41 repeat-last.** Rows never reshape: Previous|Next, H&R start|end, diversion pair (left relabels in place between start and active/update — START vs EDIT selector; End always present, `enabled = diversionActive`, i.e. `skippedStops.isNotEmpty()` — a zero-stop diversion reads as not-active, accepted), Jump-to-stop|Trigger-announcement, half-width End Journey. Disabled driver buttons are the same solid fill FADED (no border/grey swap — Rule 20 holds). The menu (`TriggerAnnouncementOverlay`, JumpToStopOverlay slot/posture, collapse-on-fire) lists every fireable announcement with its literal text and carries two deliberate contracts: **informational rows (route, per-stop next-stop) are ANNOUNCE-ONLY** (`AnnounceOnDemandUseCase` — bare bus emit; no index write, no `journey_events` row, no transient visual — the hero/tube-map stay the state-bound carrier, Rules 18/19 untouched); **regulated rows (H&R, diversion) reuse the panel buttons' state-coupled use cases** (one behaviour, two entry points). **Termination is deliberately absent** — End Journey is its only trigger. Route/stop previews mirror the render worker's composition verbatim. Never couple informational rows to state, and never add a termination row.

**Product mark — the "pinned screen" (added 2026-07-18, third session; owner-picked from a 50-option exploration):**
35. **The A13 "pinned screen" is the product logo:** the passenger display rectangle with a location pin knocked out of it, strictly monochrome (Steelglass polarity: light `#EAEAEA` mark on `#111111` ground). Supersedes #32's ticker-bar-in-ring identity resources. Two deliberate forms: the **full mark** is the identity — Android adaptive launcher (`ic_launcher_foreground.xml`: one even-odd path so the pin hole shows the ground through and the monochrome themed-icon layer inherits the knockout; 66dp safe zone) and future branding surfaces; the **favicon carries the pin element alone on the `#111111` circle disc** (`src/app/icon.svg` — a tab-slot fit decision after the screen-rect silhouette sat visibly high in the tab, NOT a second identity; keep the faint hairline-ring disc treatment). The legacy density launcher webps are deleted (unreachable at minSdk 26 — don't recreate them). **Documented in all three ledger-#27 design-system copies as of 2026-07-18** (a "Product mark — the pinned screen" signature element in the skill + `DESIGN-LANGUAGE.md`, and a new `product-mark.html` card in the Claude Design project's Brand group, taking it to 13 cards); the recorded rule is that the mark carries **its own dark ground in both themes** — a badge, never a theme-reactive surface. The other 49 explored options live in the "PDS Logo Exploration" artifact (`2e3585f6-…`).

**Tube-map at-stop / in-transit states (added 2026-07-18, fourth session; owner-designed through plan review + three glass rounds):**
36. **The tube map shows the live position, derived from the existing departure gate — and it never animates.** `StopDetectionState.pendingDeparture` is non-null exactly when the bus is inside a stop's radius, already ledger-#31 hysteresis-confirmed, so it is flow-backed (same field → the observable `gate` mirror cannot drift) and the band reads from it. **No new detection, no new geofence**: a separate 5–10 m "at the stop" radius was proposed and **rejected** — Rule 6 discards fixes whose accuracy exceeds the radius and real bus GPS is commonly 10–30 m, so it would have failed exactly when the bus was at the stop, and it would have disagreed with both the announcement (which fires on exiting the stop's own radius) and the hero. If the fill ever reads too early the lever is that stop's dashboard-authorable `proximity_radius_meters`. Three sub-decisions, each glass-earned:
    - **The at-stop rule is index-relative, not gate-tag-relative** (`atStopIndexFor`: the gate's stop counts only within ±1 of `current_stop_index`). A tag rule (`AnnounceOnDepart` = at a stop) was designed first and **refuted in plan review** — the interpreter deliberately sets `SilentReArm` on terminus arrival, and the band is exposed ~1.5 s between overlay-clear and the end screen, so it would have read "in transit to the terminus" immediately after the termination announcement. The ±1 window also fixes the origin-priming and mid-route-start offsets, rejects an arbitrary `jumpTo` and a diversion skip-walked landing, and **structurally guarantees the filled node is adjacent to `current`** — it can never land outside the FR-AT-21 window view.
    - **Exactly one element is highlighted, and it is always the live position**: an enlarged solid disc at the stop the bus is at, or the heavier highlighted active segment while moving. **The next stop gets no treatment of its own** — a hollow ring was tried and glass-rejected as clutter competing with the at-stop disc; it is identified by its bold label and the segment running into it, and lights up only on arrival.
    - **"Passed" keys off a reached frontier, never `current_stop_index`** (`TubeMapGeometry.reachedIndex`). The current index names the stop being *approached* and runs one ahead of a standing bus, which dimmed the untravelled segment out of the bus's own stop at journey start and after a manual advance. Nothing ahead of the bus may carry the passed grey.
    - **No animation on the passenger surface.** The original sketch asked for light moving along dashed lines; dashes are unavailable (Rule 20 reserves them for hail-and-ride), and beyond that a pulse would have been the first perpetual animation there — motion is a scarce attentional resource already allocated to the regulated flash and the amber warning markers. `ACTIVE_SEGMENT_STROKE_DP` is the salience lever; the `build3-gps` TODO is retargeted at a real ~30 s position tick. **Only real position data earns motion on this surface.**
    - `DetectionEffects` gains **no third index** (ledger #31's boolean precedent; Rule 18 untouched), and the journey UI **combines** the gate with the Room-sourced index into one `TubeMapUiState` rather than collecting them independently — they propagate at different speeds and would transiently render the exact mismatched pair the feature exists to distinguish.

**Passenger route identity — the designation split (added 2026-07-18, fifth session; owner-decided, deployed and glass-verified):**
37. **`display_name` is the operator's INTERNAL handle; `route_number` is the passenger-facing DESIGNATION, and one template serves both surfaces.** Reg 7(1)(a) requires "the name, number, or other label used to designate the route" and 7(1)(b)(i) the final scheduled stopping place; the ≥22 mm route line and the route announcement carry both, as `"{designation} service to {final stop}"` (screen, no terminal period) and `"This is the {designation} service to {final stop}."` (audio, terminal period for TTS prosody). **The final stop is always DERIVED from the last stop, never authored.** Load-bearing sub-decisions:
    - **"service to", not "to"** — the designation is free text (migration 026 makes it mandatory, cap 40 chars), so the wording must read correctly for both `"the X12 service to X"` and `"the Marford Green service to X"`; plain "to" fails the second. It also already matched the frozen spec and the Tier C ticker string.
    - **One canonical module: `pds-dashboard/supabase/functions/_shared/announcements.ts`**, imported by the Deno worker (`./announcements.ts`) AND by Next (`@shared/*`). `composeRouteAnnouncement` is literally `"This is the " + composeRouteLine(...) + "."`, so screen and audio cannot diverge. Verified, not assumed: `tsc --listFiles` proves the module is in the Next program **despite** tsconfig excluding `supabase/functions/**` (`exclude` only filters what `include` discovers), so it is typechecked — which is what keeps Deno-isms out of it; and the CLI bundles it into the worker (the `scrub.ts` precedent, confirmed in the deploy log). Keeping it in `src/` was rejected (reaching out of `supabase/` is outside the CLI's bundling surface), as were two copies pinned by a test.
    - **Android keeps `route_number` nullable at every layer** — non-null would be a Room schema bump (v3 + migration + the on-hardware ritual) for a column already non-null in practice, and it dissolves any cross-repo migration-ordering dependency. The fallback lives in the pure `util/RouteLineMath.designator` and is the repo's **first non-omitting passenger fallback** (elsewhere an absent value simply omits the affordance): omission would be non-compliant and a blank would render `" service to X"`, so it falls back to `display_name`. **CROSS-REPO CONTRACT — nothing binds the Kotlin strings to the TS module mechanically; change both together.**
    - **Authoring visibility ships WITH the wording, never after it** — a free-text designation an operator cannot preview is a guess at regulated content. Deploy order is likewise load-bearing: **worker before dashboard** (otherwise the preview claims wording the worker doesn't produce — the preview lies), **migration after dashboard** (otherwise blank saves fail as an unexplained generic error), **Android last** (the benign mismatch is old-screen/new-audio; the inverse pairs a correct screen with the bug still being *spoken*).
    - **The tablet mock carries its own dark palette in both dashboard themes** (`--tablet-*` tokens mirroring Android's `PassengerDark`) — it depicts a device, not a themed surface; same posture as the product mark. Reg 14(5)(a) holds inside the screen region: mixed case only, never the `chip`/caps idiom.

**Journey banner carries no route identifier (added 2026-07-18, fifth session):**
38. **The tablet's journey banner band shows the operator's company name and the control cluster — no route number.** The designation now leads the regulated route line two rows below, so the banner copy was the same value twice. `JourneyBanner` no longer takes the route at all. Synced across all three ledger-#27 design-system copies (the skill, `DESIGN-LANGUAGE.md`, and the Claude Design `tablet-journey` card — which had additionally drifted, still showing the old `"Marford to Guildhall — to Guildhall Square"` wording inside the band).

**Deployment footguns proven this session (added 2026-07-18, fifth session):**
39. **Three silent-failure modes in the render/deploy path, each now with a named check.**
    - **The version-collision trap.** `enqueue-render-job` mints the audio version from `updated_at`. A re-render that does NOT move `updated_at` overwrites the *same* Storage path, leaves `routes.audio_version` unchanged, and the tablet's missing-file-only downloader therefore fetches **nothing** — the fleet keeps playing stale audio while the dashboard reports success. **Re-render only by re-saving through the builder, and always assert `audio_version` increased and `audio_announcement_hash` changed.**
    - **`supabase db push` takes `--linked`, not `--project-ref`** (unlike `functions deploy`), and the link persists — it sat on **production** at this session's start. A wrong-environment migration is one forgotten `link` away. **Always `link` explicitly, then `db push --linked --dry-run` and read the environment and the migration list before confirming.**
    - **A new top-level dashboard page is served ANONYMOUSLY unless its path is added to `DASHBOARD_PATHS`** (`src/lib/supabase/middleware.ts`) — a path absent from that array is not a "dashboard path" and skips the auth chokepoint entirely, and the page simply renders. Page + allowlist + nav ship in ONE commit, and the gate is a logged-out request that must redirect to `/login`.

**Force re-render audio (added 2026-07-20, sixth session; owner-approved through plan review, glass + backend verified):**
40. **A force re-render is one owner-scoped RLS UPDATE on the route, then the existing enqueue — no backend change.** `triggerReRender` (single route) and `bulkReRender` (fleet-wide) both run `UPDATE routes SET audio_render_status='pending', audio_render_error=null, audio_announcement_hash=null` via the user-scoped client before calling `enqueueRenderJob`. Two load-bearing effects: **(a)** nulling `audio_announcement_hash` makes the worker's route-announcement copy-forward condition impossible (`announcementHash === stored && audio_version > 0`), forcing full synthesis — the ONLY way a **voice** change (identical text, unchanged hash) actually re-renders; **(b)** the deployed `routes_updated_at` BEFORE-UPDATE trigger bumps `updated_at`, so `enqueue-render-job` mints a NEW `audio_version` — without which the same Storage path is overwritten in place and the tablet's missing-file-only downloader fetches nothing (the ledger-#39 collision trap). Runs on `ok` and `failed` (only `pending` blocked); `bulkReRender` targets `ok`/`failed` and **skips in-flight `pending`** routes so two jobs can't race the same route's version stamp. Deployed-shape audit (staging) confirmed the enablers: `audio_announcement_hash` nullable, the trigger present, `routes_update_by_operator` RLS row-level (own route + active). Live UI updates via the client `RenderStatusPoller` (`router.refresh()` while any route pending), not Realtime. Documented in the dashboard `CLAUDE.md` FR-WD-21 gotcha. **Never retarget the version off `updated_at`, and never skip the hash-null** — either reintroduces a silent-stale-audio failure.

**Public landing page posture (added 2026-07-23, ninth session; owner-decided):**
41. **`/` is the public marketing page; the dashboard stays gated; public pages live in the `(marketing)` route group.** `'/'` is deliberately absent from the middleware's `DASHBOARD_PATHS` — the landing chrome is **session-aware, never gated** (`getUser()` in the `(marketing)` layout/page decides "Sign up" vs "Open dashboard"), and the `(auth)` layout's logged-in redirect targets `/routes`, not `/`. Future public pages (privacy, terms) join the group and inherit the header/footer. The ledger-#39 "new dashboard page must be added to `DASHBOARD_PATHS`" gotcha still stands for gated pages. The hero's `TabletDemo` is the documented **third sibling** of the tablet-depiction family (route-line mock → ticker screen → demo), each deliberately shaped by its consumer — **do not unify them**; it carries the **third documented cross-repo copy** of the terminating wording (Android `strings.xml` → `/guide` → demo; change all together) and composes the route line via `@shared/announcements` so it cannot drift. The `--tablet-flash-ground`/`--tablet-flash-ink` tokens extend the theme-invariant depiction block (values from the design language's flash row — a depiction extension, NOT a design-language change; no ledger-#27 sync). OG image deferred until a designed asset exists (pinned-screen mark the obvious basis — never a placeholder image).

**Business closure and portfolio posture (added 2026-07-26, tenth session; owner-decided):**
42. **PDS is closed as a business; the artefact lives on as a portfolio piece.** The pilot customer (a personal contact's bus company — the same company that had wanted it "asap") withdrew 2026-07-26, and with it the market thesis; the owner ended the commercial track entirely. Load-bearing consequences: **no incorporation, no Stripe activation, no payment gate, no legal drafts, no sales effort — ever, unless deliberately re-opened** (nothing was built, so nothing needs unwinding; the closure cost ≈ a domain fee). **The deployed product stays live** on `pds-dashboard.com` as the portfolio exhibit and dormant shop window — do not tear down, and keep the human review-and-push gate for any code change exactly as before. Open signup's exposure remains accepted (no marketing, no users expected). The remaining work is the **portfolio conversion** — scrub (secret history, Supabase refs, personal details in this repo) before ANY repo goes public → README/architecture/compliance story with honest AI-assisted, human-directed framing → public repos + CV/interview prep off the decision ledger — owner-paced, tracked from the owner's personal vault (`Documents\brain`, `projects/pds-cv/`), which supersedes this roadmap as the planning home for that work. This workspace's rules (compliance invariants, glass verification if the tablet is ever touched, docs disciplines) stay in force for any future session here.

**Portfolio conversion execution (added 2026-07-29, eleventh session; owner-decided):**
43. **Only what is visible in the shot list gets fixed; the landing page is now placeholder-free.** The conversion plan's scope test is the video's 17 shots — a defect not on screen does not get worked on, and the "Defer Without Guilt" list stays closed. Under that rule exactly two things were changed: the pricing panel now reads **"Pricing on application"** (section kept — cutting it leaves a hole where a B2B reader expects pricing; the figure was the one element on the page that read as a rendering bug; the claim is *exactly true*, a per-vehicle model was designed 2026-07-25 and never published) and the **owner-contact placeholder was removed rather than filled** (an address on a page about to be decommissioned dies with the domain but survives in archives; Phase 6's case study is the contact route). Consequences for future edits: the `/ vehicle / month` span is gone with the figure, the price line is no longer `font-mono` (Rule 16 reserves mono for data; a sentence at `text-3xl` wraps badly in the 448 px card), and the file-header + section comments no longer describe pricing as pending a Phase 4 task that will never happen. **Do not reintroduce a figure** without a real pricing decision.
44. **Production data is fictional and must stay that way; the demo route's last stop is load-bearing.** The pilot operator's real Wellcastle data — route, stops, 29 journey summaries, pairing codes, rate-limit rows and **all 21 audio WAVs** (which spoke the real stop names aloud) — was deleted 2026-07-29 and replaced with the RR1 demo set recorded in the anchors above. Load-bearing details for anyone touching this again: **(a)** `journey_summaries.route_id` has **no foreign key**, so deleting a route silently orphans its summaries — delete summaries first, always; **(b)** Storage is covered by no cascade and `DELETE FROM storage.objects` is blocked by a `protect_objects_delete` trigger, so audio must go via the Storage API/UI; **(c)** deleting the **auth user** is the clean lever — it cascades to `operators`/`devices`, whereas deleting the operator row leaves an orphaned auth user still carrying `operator_id` in `app_metadata`; **(d)** `auth.users.raw_user_meta_data.company_name` is a **second, unsynced copy** of the company name that nothing keeps in step with `operators.company_name`; **(e)** the tablet must be uninstalled **before** any server-side delete, because its `journey_summaries_pending` outbox will otherwise re-upload real journeys afterwards. The real privacy exposure was never the company names — it was third-party personal email addresses in `auth.users`, which the table editor exposes on camera. **Multi-tenancy, RLS and the data model were not touched** and must not be simplified for the demo (conversion plan's one hard rule — it is the expensive part and it is what the infrastructure shot is *about*). Supabase project refs were deliberately **left unscrubbed** everywhere: they are not secrets, and the prod ref appears in a Sentry issue title that the infrastructure shot films.
45. **This repo's history was rewritten on 2026-07-30 and it is publication-bound; write only the scrubbed vocabulary from here on.** All 59 commits were rewritten with `git-filter-repo` to remove the pilot customer's relationship to the owner, the pilot's location and route data, both Firebase project IDs, the owner's personal email and the local Windows username. **Anything committed here from now on ships publicly** — the substitution table is in the workspace `CLAUDE.md` (untracked, never published, and deliberately not mirrored here, because publishing the mapping would reverse the scrub); consult it before writing a session file, and note it governs **commit messages** as well as file contents. Load-bearing consequences: **(a) every SHA changed** (`2c9ae69` → `18d746d`), so short `pds-planning` hashes quoted in earlier session files and in this ledger no longer resolve — cross-repo `pds-android`/`pds-dashboard` hashes are unaffected and stay valid, and a Phase 6 README note discloses this; **(b)** the GitHub repo was **deleted and recreated, never force-pushed** — a force-push leaves pre-scrub commits fetchable by direct SHA indefinitely, so if this ever needs redoing, delete-and-recreate again; **(c)** the correct way to change the rule set is a **clean re-run from the pre-scrub mirror**, never a second pass over already-rewritten history; **(d)** author/committer names, emails and both dates are byte-identical across all 59 commits and must stay that way — the dated timeline is what makes this repo the exhibit; **(e)** the one signed commit lost its signature, unavoidable in any rewrite. Deliberately **not** scrubbed, and safe to write: the Supabase project refs, the tablet serial, the Vercel deploy URL, the Sentry org slug and monitor ID, production entity UUIDs. Pre-scrub backups (a mirror clone and a folder copy, both holding the original content) are outside the repos at `C:\Users\dev\Documents\pds-scrub-2026-07-30\` — **never copy that folder anywhere shared**, as the rules file necessarily contains the original strings. **Do not make this repo public until Phase 6** (README + case study) exists; the scrub no longer blocks it.

- **Hardware:** Lenovo TB373FU tablet, Android 16, USB serial `HA2DG5NK`. GPS testing via **Lockito** (mock-location app); accuracy must be set well under a stop's proximity radius or fixes are discarded. **Lockito accuracy gotcha (2026-07-17):** the accuracy chip is labelled "Configuration to use on **newly added parts**" — an already-drawn route keeps its original (accurate) config, so raising accuracy does NOT affect the existing route. To inject a specific accuracy (e.g. to test the coarse-GPS warning, which needs accuracy *above* the radius), draw a **fresh** route with the accuracy set first (its geography need not match the PDS route — the coarse detector keys on accuracy-vs-radius, not position). Verified end-to-end this way: ~700–1070 m fixes → "Weak GPS" at the 45 s threshold. **The tablet runs the `prodDebug` flavour for pilot testing as of 2026-07-12** — it points at the live production stack (dashboard `pds-dashboard.com` + prod Supabase `qopkjsihmdplmsoqrzji`), so both surfaces share one backend; the `staging` flavour is reinstalled only to validate schema/migration changes (never verify schema against prod). Only one flavour installs at a time (same `applicationId com.pds.application`; side-by-side deferred to Stage 4), so switching backends is a reinstall. `prodDebug` writes test data into production (device/auth rows, `journey_summaries`) — harmless pre-pilot, worth a cleanup before the pilot operator onboards. The prod build requires **`SUPABASE_URL_PROD` + `SUPABASE_PUBLISHABLE_KEY_PROD` populated in `local.properties`** (done 2026-07-12; git-ignored, local only — a blank value builds green but surfaces as "no internet" on the tablet) and `app/src/prod/google-services.json` (present, `pds-production-xxxxxx`).
- **Primary GPS/H&R test route — on STAGING** (MCP-confirmed 2026-07-29; production no longer holds any Wellcastle data, see the demo-data anchor below): `575261eb-def8-486e-b4ab-320e07a3ff17` (route `X13` "Marford to Guildhall", direction label **Inbound** since return-route generation, 6 stops, scheduled/H&R/scheduled — H&R = stops 2–3, Marford Shops + Angel Inn — 80 m radii). Its **generated return route** is `6820d7bb-c4d1-4692-b660-32c8abc77b01` (same display name, labelled **Outbound**, reversed stops, H&R mirrored) — the two carry the same display name, so fleet-cell checks distinguishing them need the UUID. A separate all-scheduled route ("C4 FCM Test") also exists; the older "Wellcastle Road to South Wellcastle" H&R route's stop geography is scattered across counties and is unusable for Lockito runs.
- **Canonical staging entities:** one operator (`360ffc45-…`) and one device (`32ab0216-…`). **Staging still carries the Wellcastle test data** (the two `X13` "Marford to Guildhall" routes above plus "Wellcastle Road to South Wellcastle") — the 2026-07-29 scrub was production-only. Nothing films staging, so this is not a blocker, but staging is NOT scrubbed.
- **Production demo data (post-scrub, 2026-07-29 — ledger #44):** three operators — **Bus Company Ltd** (`2c4f7c54-…`, owner's own login, owns everything), **Coach Company Ltd** and **Rail Replacement Ltd** (empty, on plus-addressed owner emails, existing so the multi-tenancy claim has more than one row on camera). One route: **`822467fa-cf78-46f6-a48a-d4d4e0af276e` — `RR1` "Victoria to Paddington"**, Outbound, 6 stops at 200 m radii, all `scheduled`: London Victoria Rail Station (`9100VICTRIC`) → Knightsbridge Station / Harrods (`490000130KE`) → Hyde Park Corner (`490000119X`) → Dorchester Hotel (`490006111N2`) → Marble Arch (`490000144L`) → **Paddington Station (`4900PADTON1`)**. The last stop's name is load-bearing: the worker derives the final stop from it, which is what makes the announcement read *"This is the RR1 service to Paddington Station."* and match the landing page's shared example. One device (`New Device #1`), `audio_enabled` true. Pre-scrub data is backed up locally outside the repos at `C:\Users\dev\Documents\pds-backup-2026-07-29\`.
- **Supabase:** staging ref `febcpudiorwnsbcphcng`, production ref `qopkjsihmdplmsoqrzji`. Two MCP servers — `supabase-staging` / `supabase-prod` — both permanently read-only (ledger #22); production readable, never writable, via MCP. The Supabase CLI is logged in on this machine and pinned as a pds-dashboard devDependency at 2.109.1 (read-only checks like `secrets list` / `functions list` available; `WORKER_SHARED_SECRET` is the same value on both projects). **CLI `link` is on staging as of 2026-07-20** (relinked from prod, where it had been left — ledger #39; `db push --linked` follows the link, so a wrong-environment migration is one forgotten `link` away).
- **Firebase:** two projects since 2026-07-10 — staging `pds-staging-xxxxx`, production `pds-production-xxxxxx` (FCM-only; Analytics/Gemini off). Per-flavour `google-services.json` (gitignored, local) in `pds-android` at `app/src/staging/` and `app/src/prod/`; service-account keys live in the owner's local key store (`Documents\PDS\passwords\`) and in each Supabase project's `FCM_SERVICE_ACCOUNT_JSON` secret.
- **Sentry:** org `passenger-display-system` (EU region, `de.sentry.io` ingest); projects `android` / `dashboard` / `edge-functions`, one DSN each, environment-tagged (ledger #20). Quota guards live as of 2026-07-10: spike protection enabled on all three projects; the `edge-functions error flood` metric monitor (errors > 50/hr → High-priority issue → the project's email rule, connected explicitly; email delivery empirically proven). The three default alert rules are renamed per-project (`android` / `dashboard` / `edge-functions` "— high priority issues"; 2026-07-10, verified intact post-rename). Sentry MCP connected at user scope (reads + issue updates work; project creation is owner-only). Vercel MCP also connected (deployments/logs; env vars remain dashboard-only).
- **Design system:** ledgers #32 + #27. Reference copy `living/DESIGN-LANGUAGE.md`; Claude-loaded skill at workspace `.claude/skills/pds-design-language/` (plus Anthropic's `frontend-design` and the third-party `ui-ux-pro-max` skill, installed/vetted 2026-07-17, for future explorations); browsable component library = Claude Design project at the same projectId (**14 cards**, owner's claude.ai login; the original 12 all replaced with Steelglass cards 2026-07-17 — `chips`/`glass-panel`/`mono-data` supersede `stamps`/`ticket-table`/`ephemera` — plus `product-mark` in the Brand group, added 2026-07-18 for the ledger-#35 pinned-screen mark, and `interaction.html` added 2026-07-23 for the interaction-affordance principle). The three copies sync together per ledger #27 (procedure at the bottom of the skill and DESIGN-LANGUAGE.md). Brand fonts Inter + JetBrains Mono (OFL; static TTFs bundled in `pds-android/app/src/main/res/font/`, build-time-fetched via next/font on the dashboard). Third-party skills at workspace `.claude/skills/` with provenance in its `README.md`.
- **Web hosting / domain:** dashboard hosted on **Vercel** (project `pds-dashboard`, team `ohj`), production on the custom domain **`pds-dashboard.com`** (registered through Vercel, live since 2026-07-12). **The Vercel Framework Preset MUST stay "Next.js"** — "Other" produces green-but-unserved builds that 404 every route (the 2026-07-12 bug). Deployment Protection is **Standard** (confirmed 2026-07-12): generated `*.vercel.app` URLs 302 to Vercel SSO, the custom production domain is public. `NEXT_PUBLIC_SITE_URL = https://pds-dashboard.com` is set (Production). Env vars and all deploys stay human-managed in the Vercel dashboard. **CI/CD = Vercel Git integration only** (no GitHub Actions): push `main` → prod; branch/PR → SSO-gated preview on staging Supabase (per-environment Vercel env vars). **Webhook-miss gotcha (2026-07-17):** the GitHub→Vercel webhook silently skipped one `main` push (no build created at all — not a failed build); recovery is an empty retrigger commit pushed to `main`. After any push, confirm a new deployment exists (Vercel MCP `list_deployments`) rather than assuming; if a second miss occurs, inspect the GitHub webhook deliveries. **Web Analytics** is live (`@vercel/analytics`, cookieless); Speed Insights deliberately off (paid, low value for an internal tool).
- **Email / SMTP:** production sends **PDS-branded** auth email via **Resend** custom SMTP (domain `pds-dashboard.com` verified with DKIM/SPF/DMARC in Vercel DNS; `smtp.resend.com:465`, user `resend`, API key = password, sender **PDS** `<noreply@pds-dashboard.com>`; email rate limit 30/h). Configured on the **prod** Supabase project only — **staging stays on the default Supabase sender** (rate-limited ~2–3/h, fine for solo testing; "email didn't send" on staging is almost always that cap). The reset/confirmation templates are the Supabase defaults (owner likes them; only the sender identity was branded). Resend account on the owner's Google login.
- **Local repo paths:** `C:\Users\dev\Documents\pds\pds-android` and `…\pds-dashboard`. (The workspace parent folder was renamed from `GitHub` to `pds` on 2026-07-29.)
