# Session: 2026-07-29 — Portfolio conversion: Phase 1 (landing polish) and Phase 2a (production data scrub)

**What changed:** First execution session of the portfolio conversion plan (`Documents\brain`, `projects/pds-cv/conversion-plan.md`). **Phase 1** removed the two placeholders left on the public landing page, both of which are on camera in the demo video's opening scroll: the `£—` pricing figure became real copy ("Pricing on application"), and the owner-contact placeholder comment was deleted rather than filled. Both shipped and are live on `pds-dashboard.com`. **Phase 2a** scrubbed the production database of the pilot operator's real route and stop data and replaced it with a fictional demo operator set and a filmable demo route. Production now holds three fictionally-named operators, one route (**RR1 · Victoria to Paddington**, six real London NaPTAN stops), fresh rendered audio, and the re-paired tablet. Phase 2b (the `pds-planning` git-history scrub) was planned and audited but deliberately **not started** — it is the one irreversible step and gets its own session.

**Commits:**
- `pds-dashboard` `8817749` — chore: replace landing-page pricing placeholder with "pricing on application"
- `pds-dashboard` `e4cd749` — chore: remove owner-contact placeholder from landing-page CTA

Both reviewed by the owner and pushed (owner-authorised a single Claude-run push; the harness classifier blocked it, so the owner ran it). Vercel production deploy `dpl_Dr2ghwQ…` READY for `e4cd749`, live-verified on the domain in both themes.

**Decisions made:**
- **Pricing reads "Pricing on application", section kept.** `£—` read as a rendering bug — the one element on the page that said "unfinished". Cutting the section would leave a hole exactly where a B2B reader expects pricing. "On application" is standard early-stage posture and is *exactly true*: a per-vehicle model was designed 2026-07-25 and never published. The `/ vehicle / month` span went with the figure (it shared the line and reads as nonsense after a prose substitution); typography stepped down from 30 px bold monospace to 20 px semibold sans, since Rule 16 reserves mono for data and a sentence at display size wraps badly in the 448 px card.
- **Contact line removed, not filled.** The site is being decommissioned: an address here dies when the domain lapses while persisting in any archive that outlives it. Phase 6's case study is the contact route.
- **Supabase project refs dropped from the 2b scrub scope.** They are not secrets (every client request carries the ref), and the prod ref appears in a Sentry issue *title* that Shot 16 films — scrubbing it from the repo while filming it made no sense. Removes the single largest chunk of the history rewrite (199 occurrences).
- **Test-operator accounts deleted and replaced, not renamed.** The real privacy exposure was never the company names — it was two third-party personal Gmail addresses on `auth.users`, which the Supabase table editor exposes in Shot 16. Deleted via the auth admin API (cascades cleanly); two fictional operators re-created by normal signup on plus-addressed owner emails, so the multi-tenancy claim still has more than one row behind it.
- **Journey summaries deleted, to be regenerated by driving.** Shot 15 films "the journey summary the tablet just uploaded", so real demo-route journeys during Phase 3 will repopulate the table honestly rather than re-pointing 29 fabricated rows at a route that never ran them.
- **Demo route pre-built rather than authored first on camera.** Proves render + sync before shoot day; the on-camera authoring in Shots 3–5 becomes a rehearsed re-create.
- **Stop 1 is "London Victoria Rail Station", not "Victoria Station"** — forced by a product limitation, see Banked.
- **Phase 2b scoped out of this session.** Force-push with no undo; wants a clean head, its own backup, and its own plan.

**Verified:**
- Landing page: `npm run typecheck` + `npm run build` clean; pricing panel and CTA checked at 1440 px in **both** Steelglass themes locally, then re-verified on the live production domain after deploy. Visitor-state header confirmed rendering `Log in / Sign up`.
- Logged-in header transition (`Sign up` → `Open dashboard` → `/routes`) **exercised on the live site by the owner** — it had never been verified in production before this session.
- Production scrub confirmed read-only via `supabase-prod` MCP: zero rows matching Wellcastle NaPTAN prefix `9990`, zero routes matching `X12`/`Marford`/`Guildhall Square`, zero journey summaries, zero pairing codes, zero rate-limit rows, zero third-party emails in `auth.users`, one auth identity per user (no orphans), NaPTAN reference data intact at 375,730 rows.
- All 21 Wellcastle audio WAVs deleted from the `route-audio` bucket (one 0-byte `.emptyFolderPlaceholder` remains — folder marker, no content).
- New route rendered end-to-end: `audio_render_status = ok`, `audio_version` stamped `1785359329458`, no error, both content and announcement hashes computed, **7 WAVs** at the new version path. The announcement composes to *"This is the RR1 service to Paddington Station."*, matching the deployed landing page's example.
- Tablet reinstalled (`prodDebug`), re-paired, `activation_state = active`, `audio_enabled = true`, `fcm_token` present — both gates that silently produce a mute tablet. **Owner glass-confirmed RR1 appears in the tablet's route list.**

**What's next:** **Phase 2b — the `pds-planning` history scrub**, in a fresh session. Then the mock-GPS track (the last precondition before recording), then Phase 3 capture.

**Banked / open:**
- **NaPTAN search cannot reach a stop whose name is a common token pair.** `searchNaptan` orders by `stop_name` and caps at 20, so the stop named exactly "Victoria Station" (`4900VICTRIC0`) ranks 69th of 88 for every query that matches it — its only tokens are `victoria` and `station`, so no query can isolate it. Searching a stop's exact name does not return that stop. Real defect, deliberately **not fixed** (product is closed; not in the shot list). Worked around by using `9100VICTRIC` "London Victoria Rail Station", which suits a rail-replacement demo anyway. Only the *last* stop's name feeds the announcement, so this is cosmetic.
- **Staging still holds the Wellcastle test data**, including the primary GPS/H&R test route `575261eb-…` ("Marford to Guildhall") and its generated return. Untouched by this scrub, which was production-only. Nothing films staging, so this is not a blocker — but it is not scrubbed.
- **Supabase org is on the FREE plan** (observed in the dashboard, org "Horizon Display Systems"). The Phase 5 teardown list assumes a paid Supabase tier; if it is genuinely free, "pause rather than delete" stops being about cost.
- **Google Cloud billing question answered.** A billing account exists behind the TTS project (`pds-production-xxxxxx`), pay-as-you-go, **£0 of £222 credits used**, credits expiring 2026-08-16. Nothing has ever charged. Phase 5 should check the billing account's **linked-projects list** (Firebase projects are GCP projects too), not just the TTS project.
- **Hand-applied production schema changes: none.** Owner-confirmed; a deployed-schema-vs-migrations diff was offered and remains available but was not run.
- **2b audit complete, findings banked** (see the STATE.md ledger entry): 58 commits not 57; the pilot company is **never named anywhere** — only the relationship, which appears in 16 commits across 5 paths **plus one commit message** (`ce8afe1`) that a file-contents-only rewrite would miss; a second, differently worded phrasing that a naive single-string replacement rule would skip; `.claude/settings.local.json` recoverable from history and leaking the Windows username 9×; commit metadata already clean (all 58 authored by the GitHub noreply alias); **zero real secrets** across all history (every hit was a variable *name* in a docs table); `git filter-repo` not installed, though Python 3.12, pip and Git 2.53 all are.
- Demo operator names were initially placeholder-ish and were renamed by the owner to **Coach Company Ltd** and **Rail Replacement Ltd** (alongside the retained **Bus Company Ltd**).
- Local backup of everything deleted: `C:\Users\dev\Documents\pds-backup-2026-07-29\pds-prod-data-backup-2026-07-29.json` (73 rows + Storage manifest; contains `device_secret_hash` and `fcm_token` — private, never commit). WAV bytes were **not** backed up, only the file list.
