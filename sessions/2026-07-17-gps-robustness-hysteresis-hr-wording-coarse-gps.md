# Session: 2026-07-17 — GPS robustness: geofence hysteresis, H&R timing/wording, coarse-GPS warning

**What changed:** Three pieces of GPS-detection robustness, all glass-verified on the Lenovo (`HA2DG5NK`, prodDebug) and pushed. (1) **Pushed last session's model-2 commits** (`0e54d0f` + `69bc27c`) after owner diff-review + a clean-run glass sign-off. (2) Built **temporal-hysteresis confirmation** on GPS stop detection — the flagged looping-route robustness fix — and glass-verified it kills the early look-ahead misfire on the exact path that broke last session, with no regression. (3) **Verified H&R under model 2 was compliant** (the other open item), found the START *wording* (not timing) was off, and **reworded it "now entering" → "approaching"** (+ re-rendered the bundled WAV). (4) Built the deferred **coarse-GPS / detection-starved driver warning** ("Weak GPS") and glass-verified the full STARVED→RECOVERED lifecycle.

**Commits (all `pds-android`, pushed):**
- `pds-android 46697b2` — feat: confirm GPS geofence transitions over consecutive fixes (FR-AT-13, ledger #31). Pure `confirmDecision` wrapper over `decideStopDetection` (untouched — no test churn): an actionable geofence transition (arrive/look-ahead/depart) must persist over `CONFIRM_FIXES=3` consecutive qualifying fixes (`MAX_FIX_GAP_MILLIS=6s` continuity guard) before it acts; the gate state IS the accrual memory; a mid-accrual NoOp is refuting (accurate → reset) vs indeterminate (coarse → HOLD — the compliance-safe direction). `K=1` disables it (byte-for-byte prior behaviour). Wired in `ProcessLocationFixUseCase` (carry accrual in `StopDetectionState.pendingConfirmation`), `fixGap` threaded from `LocationService`, cleared on manual advance. 8 files, +StopDetectorConfirmationTest (12) +ProcessLocationFixUseCaseTest (2).
- `pds-android bc70f73` — fix: reword H&R start announcement to "approaching" to match Reg 11 timing (FR-AT-26). Overlay string + re-rendered `hail_and_ride_start.wav` (Google TTS `en-GB-Neural2-B`/LINEAR16/24kHz via a one-off script reading `GOOGLE_TTS_API_KEY` from `pds-dashboard/.env.local`); the re-render also fixed a latent **double-WAV-header** in the June asset (produced a clean single-header WAV).
- `pds-android 6d26f78` — feat: warn the driver when GPS is too coarse to detect stops (ledger #31). New pure `CoarseGpsStateMachine` (OK/STARVED, no ACQUIRING — the 45s sustained-run duration is the acquisition guard) + `@Singleton CoarseGpsMonitor` (mirrors `GpsSignalMonitor`), per-fix in the `LocationService` collector (no watchdog), `@Volatile` run stamp + pure `isStarvedFix` predicate (mirrors the detector's armed/disarmed gate branches, single-sourcing `passesAccuracyGate`), two-surface amber marker ("Weak GPS", parallels "No GPS", hidden while "No GPS" shows), `COARSE_GPS_DETECTED`/`_RECOVERED` events, reset on both journey seams. 13 files, +CoarseGpsStateMachineTest (8) +CoarseGpsDeciderTest (10).

**Decisions made:**
- **Geofence hysteresis is the looping-route fix (ledger #31 hardening).** Confirm actionable transitions over K=3 fixes; the coarse-fix HOLD keeps a jittery reading from wiping a legit accrual (never *delays* a required announcement beyond the bounded confirmation window; termination is backstopped by the stationary auto-timeout, so nothing is dropped). Also fixes the symmetric single-fix jitter-departure. K=3 glass-confirmed well-tuned (every real stop gathered 3+ in-radius fixes).
- **H&R START timing is correct and legally required where it is — do NOT move it.** Reg 11(2)(a) mandates the "entering" info **at the last scheduled stop *prior* to the H&R section**, and 11(1) frames it as forewarning ("will be commencing"). The owner's instinct to fire it *at* the first H&R stop would have **broken** 11(2)(a). The real mismatch was the present-tense *wording* ("now entering") firing before the section — fixed by rewording to "approaching". END ("now leaving", fires on departing the last H&R stop) is accurate and unchanged.
- **Coarse-GPS "Weak GPS" warning (ledger #31 follow-up).** Fires after a 45s sustained run of fixes that fail the accuracy gate for the current stop(s) — the silent-journey gap the GPS-lost warning misses (it keys on fix *absence*). Non-dismissible (auto-clears on an accurate fix); driver-only; never touches the announcement path. Owner picked terse "Weak GPS" over the longer "…stops may not announce" for parallelism with "No GPS".
- **Amended `6d26f78` in place** (unpushed) to fold the wording refinement rather than add a noise commit — one clean logical commit, owner-reviewed before push.

**Verified:**
- **Unit:** full `stagingDebug` suite green after each commit (+32 new tests across the three).
- **Glass (`HA2DG5NK`, prodDebug, Lockito) — per-fix logcat traces captured, read, and stripped before commit each time:**
  - *Hysteresis:* the identical looping path that misfired last session now runs clean end-to-end (no early look-ahead); clean straight run no-regression; K=3 reached by every stop.
  - *H&R:* clean pass under model 2 — START on departing the last scheduled stop (`boundary=START@Marford Shops`), next-stop suppressed across both H&R stops, END on departing the last H&R stop (`boundary=END@Fairfield Street`); owner sound-checked the reworded "approaching" audio.
  - *Coarse-GPS:* with mock accuracy ~700–1070m (radius 100m), `starved` accrued and STARVED fired at `runMs=45998` (the 45s threshold, marker shown), then a single accurate fix (28m) drove RECOVERED. Also surfaced a **Lockito gotcha**: its accuracy chip is "config for **newly added parts**" — an existing route keeps its original accurate config, so a *fresh* coarse route was needed to inject coarse accuracy.

**What's next (FIRST on the agenda next session — owner-requested):** the **remaining deferred edge cases** surfaced this session, none blocking but worth clearing before the pilot: (1) **overlapping geofences** on closely-spaced consecutive stops (bursty / zero-length legs); (2) **single-fix multi-stop skip** on a fast leg (the look-ahead recovers only one; manual advance is the fallback); (3) **duplicate-coordinate stops** on a physical loop (`findStartCandidate` picks the first match); (4) a **dashboard authoring note** — use tighter per-stop radii on looping/close routes. Then the big remaining build item, the **payment gate** (What's Left #7).

**Banked / open:**
- All three commits pushed to `origin/main`; no code debt.
- Coarse-GPS optional visual sub-checks not explicitly glass-hit (all low-risk, same paths as the proven "No GPS"): panel-header re-surface, and the "No GPS replaces Weak GPS" precedence. No chime/flash is structurally guaranteed (off the announcement path).
- `COARSE_THRESHOLD_MILLIS=45s` and hysteresis `CONFIRM_FIXES=3` / `MAX_FIX_GAP_MILLIS=6s` are glass-tunable consts (both confirmed appropriate this session).
- One-off TTS render script (bundled-asset re-render) lives in the session scratchpad only — not committed; re-derivable from `audio-render-worker`'s TTS config if another bundled phrase ever needs re-rendering.
