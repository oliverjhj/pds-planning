# Session: 2026-07-23 — Phase 3 ticker/marquee simulator + design-language interaction-affordance

**What changed:** Shipped the second and final Phase 3 item — the ticker/marquee simulator on `/guide`, an authoring aid that (for a chosen tablet screen size) shows whether the passenger route line and next-stop hero sit static or **marquee**, mirroring the tablet's ticker engine. Then, as a small design-doc task, added an "Interaction & affordance" section to the design language and synced all three ledger-#27 copies. Also confirmed the workspace-root `.playwright-mcp/` is already clean (no-op — nothing to remove).

**Ticker/marquee simulator (Phase 3), all in `pds-dashboard`, additive and dashboard-only:**
- New pure, unit-tested `src/lib/ticker-sim.ts` (+ `.test.ts`, 12 tests): `screenWidthMm` (diagonal + aspect → physical width), `emMmForCapHeight`, `verdictFor` (fits / borderline / scrolls), `SCREEN_PRESETS`. Ticker constants (`MIN_PASSENGER_TEXT_MM`, `COMPLIANCE_MARGIN_MM`, `TICKER_SPEED_MM_PER_SEC`, `TICKER_INITIAL_DELAY_MS`, `TICKER_GAP_MM`) + `HERO_CANDIDATE_MM` copied from the Android repo with a source-of-truth citation — a new accepted cross-repo copy (same posture as the fixed-announcement strings in `guide/page.tsx`).
- New `src/components/routes/tablet-ticker-screen.tsx` — a higher-fidelity device depiction that renders the route line + hero at a 22 mm-scaled size and marquees on overflow. Deliberately SEPARATE from the truncating `TabletRouteLineMock` (left untouched; its 2 callers undisturbed).
- New `src/app/(dashboard)/guide/_components/ticker-simulator.tsx` — client island: inputs (designation, final stop, a stop name), preset + custom screen picker, live in-browser Inter cap-height + width measurement (canvas, mirroring the tablet's `measureCapHeightFraction` technique), the hero rung step-down, per-line verdict badges. Route-line wording reuses `@shared/announcements` so the preview can't drift.
- One new section wired into `guide/page.tsx` ("Will my route and stop names fit?").
- **Key insight:** the marquee decision needs only the physical screen WIDTH — resolution cancels (text and screen scale by the same px/mm) — so the tool asks for diagonal + aspect, never DPI. Honesty guards baked in: a ±3% "borderline" band for the browser-vs-Android metric difference, and a 4%-per-side inset (`TICKER_SIDE_INSET_FRACTION`) standing in for the journey layout's ticker padding; both err toward warning.

**Interaction-affordance (design-doc task):**
- New "## Interaction & affordance" section in the design language: the *principle* (clickable things look clickable; the central `globals.css` cursor rule is one worked example, not the whole), the clickable-`<div>`-with-no-`role` gap the CSS structurally can't reach (→ a real `<button>`/`<a>`, or `role="button"` + `tabIndex` + a key handler, so affordance and assistive tech both hold), focus-visible (never stripped), and disabled-never-advertises-a-click.
- Synced all three ledger-#27 copies: the `pds-design-language` skill `SKILL.md`, `living/DESIGN-LANGUAGE.md` (bodies verified byte-identical), and a new `interaction.html` Claude Design card depicting default / hover / focus / pressed / disabled in both themes (a dedicated card was the right artefact — it shows the visible state treatments, sidestepping the earlier "a card can't show a cursor" objection). Project now **14 cards**; corrected a stale "12 cards" count in both docs (product-mark had taken it to 13 without a count bump).
- No Rule 16 / Rule 20 change — the principle generalises what those rules already mandate.

**Commits:** `pds-dashboard` `bfbfc94` — feat: ticker/marquee simulator on /guide (Phase 3) — pushed, Vercel production deployment confirmed for the SHA (no webhook miss). `pds-planning` (this close-out) — session file + STATE.md + the `DESIGN-LANGUAGE.md` interaction-affordance section. `SKILL.md` is a workspace-local file (workspace root is not a git repo), so its change is live for Claude immediately and is not committed anywhere.

**Decisions made:** Simulator lives on `/guide` (not inline in the route builder) and takes presets + a custom size (owner-decided). Scoped to sideways overflow only — the height-driven `PassengerTierMath` degradation is deliberately OUT and captioned as such, so its absence isn't read as "everything fits". A dedicated `interaction.html` card, not a rider on an existing one.

**Verified:** `npx vitest run` 12/12 green; `npm run typecheck`, `npm run lint`, and `npm run build` all clean (15 pages generate, `/guide` included). Vercel production deployment created for `bfbfc94` (MCP-confirmed). **Owner cross-checked the simulator against the real Lenovo TB373FU — the marquee/static boundary matches the tablet; perfect, no calibration change needed.** Design-language bodies diffed byte-identical from the intro line onward; the new Claude Design card confirmed present (14 files). `.playwright-mcp/` confirmed absent workspace-wide.

**What's next:** Phase 4 — the public landing page (a pre-login front page inside the existing Next app), the hardware-recommendation document (ruggedisation? is 13″ the size? min spec) then sourcing a one-off test unit + mount and a large supplier, legal drafts (privacy + signup T&Cs, plus a UK IPO trademark screen for the pinned-screen mark), the pitch pack, Play Store approval-path research, and the pricing model.

**Banked / open:** The ticker-constant cross-repo copy is a new accepted residual drift risk (mitigated by the source-of-truth citation; change both repos together). The simulator's `TICKER_SIDE_INSET_FRACTION` (0.04) and the ±3% verdict tolerance are the single-constant calibration levers if a future tablet cross-check ever disagrees. Height-tier simulation remains a possible later extension. `.playwright-mcp/` re-accumulation stays a close-out watch item even though it was clean this session.
