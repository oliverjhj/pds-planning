# Session: 2026-07-26 — Hardware guide, landing polish, and the business closure

**What changed:** A two-day session (2026-07-25/26) in two very different halves. First half: Phase 4/5 strategy talked through and the **hardware guide shipped as a public page** — owner-decided page-over-PDF (recommendations rot; the page doubles as the BYO sales pitch; print CSS gives the dated-PDF snapshot from one source). `/hardware` went live on `pds-dashboard.com` in the `(marketing)` group with a July-2026-researched spec list (GMS-certified Android 8.0+, GPS, BT/3.5mm audio, cellular recommended, 11–13″), three tablet tiers (Tab A11+ budget / Tab P12 "what PDS is developed on" / Tab Active5 rugged with an honest 8″ caveat), mounts (universal clamp vs RAM Mounts), 12/24V power guidance, speakers, and an itemised ~£240-per-vehicle cost strip. Then a run of owner-driven landing polish: nav reordered so the real page trails the hash anchors as "Hardware guide"; the PDS wordmark now always returns to the top of the public landing page (dashboard wordmark → `/`; a `MarketingLogo` client component handles the same-pathname no-op Next quirk); a site-wide **top progress bar** (nextjs-toploader in the root layout, muted-foreground token, no spinner/glow, hash-anchors excluded) fixing the no-feedback-double-click problem; and the **TabletDemo reworked onto a London rail-replacement route** — RR1: Victoria Station → Knightsbridge, Harrods → Hyde Park Corner → Park Lane, The Dorchester → Marble Arch → Paddington Station — shared as the coherent example across the demo, `/guide`'s SAMPLE, and the ticker-simulator defaults, with bigger text throughout, the loop opening at stop 1, the journey reset happening *under* the terminating overlay (no post-overlay flick), and a descender-clipping fix (text-5xl sets line-height 1; leading-tight cures it).

Second half, 2026-07-26: **the business was closed.** A personal contact's bus company — which WAS the pilot customer — backtracked after the weekend pitch conversation. With the only customer gone and the owner's own market read (deadline-adjacent operators either already equipped, buying premium integrated hardware, or too price-sensitive), the commercial track was closed entirely: no incorporation, no payment gate, no sales effort. The product **stays live and deployed** as a portfolio piece and dormant shop window. PDS pivots to a CV/portfolio conversion, now tracked from the owner's new personal knowledge vault (`C:\Users\dev\Documents\brain`, created this session, outside this workspace — its `projects/pds-cv/STATE.md` holds the conversion plan: close-out → scrub → README/story → public).

**Commits:** (all `pds-dashboard`, all owner-reviewed then pushed at owner instruction, all Vercel-deployed READY and live-verified)
- pds-dashboard `d21ccea` — feat: public /hardware hardware guide page + sitemap entry
- pds-dashboard `f117d3f` — feat: link the hardware guide from marketing nav and landing copy
- pds-dashboard `f4870f7` — feat: print styles for the marketing pages
- pds-dashboard `f03b612` — fix: hardware guide link goes last in the marketing nav, labelled as a guide
- pds-dashboard `97a2068` — feat: PDS wordmark always returns to the top of the public landing page
- pds-dashboard `1d32eeb` — feat: top progress bar for navigation loading feedback (adds nextjs-toploader dep)
- pds-dashboard `d0c065b` — fix: progress bar rides muted-foreground, not full-contrast foreground
- pds-dashboard `e92414f` — feat: London rail-replacement demo route, bigger demo text, loop restarts at stop 1
- pds-dashboard `bbc9900` — fix: demo resets under the terminating overlay; all demo text a step bigger
- pds-dashboard `91c298c` — fix: demo hero descenders uncropped; route line up another step

**Decisions made:**
- **BUSINESS CLOSED 2026-07-26** (ledger #42): pilot customer withdrew; commercial track ended; product stays deployed; portfolio pivot. Phases 5–6 cancelled; Phase 4's remaining commercial items (legal, pitch pack, Play Store, pricing) cancelled.
- The Phase 4/5 commercial decisions made earlier in the session, recorded for the archive though **superseded by the closure**: graduated per-device pricing (£20 base incl. 1 device, +£10/device 2–10, +£5/device 11–20, 21+ contact-us; monotonic, no cliffs); 30-day no-card 1-device free trial with trial-expiry mapped to the suspended-style read-only posture; billing counts *active* devices; no VAT until registration (~£90k threshold); underprice-now-raise-later posture; Stripe in test mode until real users (no sole-trader activation — entity change would force a new Stripe account); Ltd-before-live-payments with 1st Formations (£39+VAT registered office + £26 service address, same-day scan) researched; ImprovMX for sales@/support@ forwarding. None of this was built; nothing needs unwinding.
- Hardware doc is a living public page, not a dated PDF; print CSS is the sanctioned snapshot; consumer-grade tablets recommended over rugged (GMS requirement + cost + rugged form factors run small).
- Marketing nav convention: hash anchors mirror the landing section order; real pages trail the list named as destinations ("Hardware guide"); future /privacy//terms join that trailing group.
- Demo/guide/simulator share ONE example route (the London RR1 run) — cross-referenced at each site; change together.
- No trial wording anywhere on the site (no trial exists); CTA labels stay the landing's auth-aware pair.
- Top loader colour is `--muted-foreground` (ambient feedback, not an alert), owner-tuned from full-contrast foreground.

**Verified:** No Android changes — no glass work needed. Dashboard: build/typecheck/lint green throughout; Playwright production-build passes per change (anonymous `/hardware` 200 with `/routes` still 307→login; cross-page anchors; both themes; print emulation — light tokens, chrome hidden, no blank Reveals; reduced motion; demo loop sequence incl. frame-sampling across the overlay lift; descender box metrics; toploader mid-nav computed style in both themes, no stranded bars on anchors or the logo path). Every push confirmed as a READY Vercel deployment (webhook-miss check) and live-verified on the domain.

**What's next:** Nothing scheduled in this workspace. The portfolio conversion is owner-paced and tracked from the brain (`projects/pds-cv/`): 1) this close-out (done), 2) scrub review before any repo goes public (secret history, Supabase refs, personal details in pds-planning), 3) README/architecture/compliance story with honest AI-assisted-human-directed framing, 4) interview-prep on the decision ledger. The deployed site stays up as the live exhibit.

**Banked / open:** Dashboard `CLAUDE.md` refresh now trails further (the `(marketing)` group, `/hardware`, print styles, MarketingLogo, toploader, the RR1 example route) — fold into the conversion's docs pass. Landing-page owner-fill placeholders (pricing figure, contact email, footer © name) and the OG image are moot-or-deferred under closure; the pricing card could arguably be simplified during the conversion. The e20fe6d-era glass posture and all compliance invariants unchanged. `.playwright-mcp/` swept repeatedly during the session and clean at close.
