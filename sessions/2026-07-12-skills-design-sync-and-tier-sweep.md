# Session: 2026-07-12 — Design-card sync, third-party skills, and the emulator tier sweep

**What changed:** A no-code-commits session in three parts: (1) the Claude Design project was brought back in sync with the 2026-07-11 tickers/tiers/controls reality and a standing three-way sync procedure was written down; (2) vetted third-party skills were installed at the workspace; (3) the two banked tier-layout verification nits were discharged as far as software can take them via a temporary emulator screenshot harness, and the ticker/tier/banner code got a three-lens fresh-eyes review using the new skills (findings banked, nothing fixed).

1. **Claude Design cards refreshed (4 of 12):** `theme-toggle` (driver-panel-header wording → app-wide top-end cluster, scheme-drawn chips), `driver-panel` (toggle removed from the header mock; full-control-stack shape noted), `tablet-journey` (rebuilt: banner band + full-width single-line tickers incl. the "— to [Final Stop]" Reg 7(1)(b) carrier), `ephemera` (wordmark dashboard-only; Android `ephemera` palette role + its contrast/hue guard). Pushed via DesignSync to project `89aaa8cf-5142-44e3-ae7b-18e7b34ed8b3`.
2. **Three-way sync procedure (owner-mandated):** any design/colour/idiom change updates ALL THREE copies in the same session — the workspace `pds-design-language` skill, `living/DESIGN-LANGUAGE.md` (word-for-word identical body), and the Claude Design project cards (DesignSync: list_files → get_file → finalize_plan → write_files; `@dsCard` markers, no re-registration). Written as an identical "Sync procedure" section at the bottom of BOTH the skill and DESIGN-LANGUAGE.md. Repo Rules 16/20 outrank all three copies.
3. **Third-party skills installed** at workspace `.claude/skills/` (all vetted — suspicious-pattern scan + full read of broad-trigger skills — before install): the chrisbanes/skills Compose+Kotlin pack (12 compose + 4 kotlin + router; commit `a1c5dca`), supabase/agent-skills (2 skills; `1ad9aae`), and the anthropics/skills document skills docx/pdf/xlsx (`9d2f1ae`). **Deliberately excluded:** `shepherd` + `implement-issue` (autonomous PR/issue workflows conflict with the human-gated push posture) and `kotlin-multiplatform-expect-actual` (not KMP; false-trigger risk). **Local edits (preserve on re-sync):** a PDS override note atop the `supabase` skill (MCP read-only, writes human-gated — its execute_sql/db-push schema workflow does not apply) and the router's rows for excluded skills removed. Provenance record: `.claude/skills/README.md`.
4. **Emulator tier sweep (owner-away, zero-touch):** a TEMP instrumented screenshot harness (Pixel_Tablet AVD, headless; the real tablet untouched) rendered the real passenger-layout composables under the real `PassengerTierMath` inputs with fake journey data — 26 self-labelling captures across sizes, both themes, diversion on/off, incl. Lenovo-density (213 dpi) simulations. **Results: the full monotone tier ladder is photographed and correct** (A full stack → B1 compact 72 dp band + caption at ~850 dp sim-height → B2 caption-dropped/label-inlined at ~830 → C combined ticker at ~720); **the 72 dp compact tube band is now eyeballed** (nodes, H&R dashes, halo, label — clean); **Tier C's Reg 10(1) "Not stopping at:" suffix was captured mid-marquee at the 22 mm floor.** One harness-only bug found and fixed mid-sweep (text sized by real dpi while tier math used simulated dpi — product code never wrong). Tube-map edge labels absent from captures is by design (current-stop always, neighbours only in windowed mode). All scaffolding stripped: JourneyScreen.kt reverted byte-identical to `main`, temp test deleted, compile re-verified green, emulator apps uninstalled, AVD shut down.
5. **Three-lens review of the ticker/tier/banner stack** (three parallel agents, each loading different chrisbanes skills + the repo rules). Unanimous verdict: disciplined code, zero compliance-rule violations, marquee + teleprompter correctly out of the composition hot path (hours-long kiosk rendering recomposes only on real events). **Banked findings, ranked:** (a) the regulated-overlay scroll/hold `LaunchedEffect` is keyed only on the text — a mid-overlay viewport/ppmm change could report a stale hold (compliance-adjacent; rare on an orientation-locked kiosk); (b) the 1.25 line-height multiple exists in both `PassengerTierMath` and `PhysicalText`, tied only by a comment; (c) `calibrationPpmm` is null until an async load, so a calibrated tablet's first frames use the DisplayMetrics fallback; (d) test gaps — the Tier-C combined-ticker string assembly (sole Reg 10(1) carrier) is untested, the ticker/overlay speed constants aren't literal-pinned, the ticker overflow boundary is tested one-sided; (e) minor polish — admin PIN digits transiently in the saved-instance Bundle via `rememberSaveable`, a composition-time state back-write in the overlay, un-memoised hero measure ladder, `rememberPhysicalTextStyle` doesn't remember, `DisposableEffect(Unit)` keyings, missing modifier params, a qualified-yes on `Mm`/`Px` value classes confined to util.

**Commits:** pds-planning (this close-out commit only) — session file, STATE.md, DESIGN-LANGUAGE.md sync-procedure section. **No code-repo commits; nothing for the owner to push.**

**Decisions made:**
- The three-way design sync rule (item 2) is standing procedure.
- Third-party skills are adopted with deliberate exclusions and local override notes; workspace/repo rules always outrank an installed skill.
- Phone-portrait observation accepted: the tier plan degrades on height only (no width lever) — at 360 dp width a 22 mm line shows ~2 characters at a time; fine while phones are non-deployable under Reg 14(2).

**Verified:** Emulator/screenshot-verified only (not hardware): all four tier states, both themes, diversion tube-map strikethroughs, Reg 10(1) Tier-C carrier, 72 dp compact band. `compileStagingDebugKotlin` green after scaffolding strip; `git status` clean in both code repos. The physical 22 mm ruler check mid-scroll and the Lockito Tier A diversion pass remain owner-on-glass items.

**What's next:** The pilot (unchanged). Optional: a small "review-findings" fix slice for findings (a)–(d) — plan-mode gate, one commit each; only (a) would need glass re-verification (regulated overlay path).

**Banked / open:**
- 22 mm ruler check on the tickers mid-scroll + Lockito diversion pass at Tier A — still owner-on-glass, when convenient.
- The review findings list above — non-blocking candidates for a future slice.
- Sweep screenshots live in the session scratchpad (transient); the harness pattern (instrumented screenshot test, density-simulated cases, plan-labelled filenames) is reproducible from this file if ever needed again.
- Tier B/C still never seen on REAL small hardware (none deployed — fine); the emulator sweep is the software-side discharge of that nit.
