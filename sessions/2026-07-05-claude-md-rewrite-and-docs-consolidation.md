# Session: 2026-07-05 — CLAUDE.md rewrites + frozen-doc consolidation

**What changed:** First session in the restructured workspace. Oriented and audited the docs against the code, which surfaced that `pds-planning/historical/` held stale **v3.7** planning docs while `pds-android/docs/` held the final frozen **v3.9** (the banners already claimed v3.9) — the archive was refreshed to the v3.9 bodies (banners preserved byte-identically) before both repos' `docs/` folders were deleted. Then STATE.md item 1: both repo CLAUDE.md files rewritten lean and code-grounded. Android (768→681 lines) gained the four previously-undocumented feature areas (diversions, kiosk L1, the admin store-triplet pattern, journey-summary upload as SyncManager stage 5) and lost the changelog/sweep archaeology, the DO-NOT-READ regime, and the retired architect/builder workflow. Dashboard (607→522 lines) got the critical `audio_version` model correction (the old "equals `updated_at`" framing is the one the C3 audit proved produces 404ing audio paths), Next 16/`src/proxy.ts` reality, deployment state through migration 024 on both environments, a migration ledger, and a known-stale-text gotcha. The Next-tooling-generated `AGENTS.md` was also deleted (owner call; regeneration by Next tooling is acceptable). Everything committed and pushed to all three remotes.

**Commits:**
- pds-planning `2af75c4` — docs: refresh historical planning docs from v3.7 to the final v3.9 bodies
- pds-android `7254e78` — docs: rewrite CLAUDE.md against the built codebase (lean, code-grounded)
- pds-android `2489345` — chore: remove frozen planning docs (v3.9 archived in pds-planning/historical; WORKFLOW retired)
- pds-dashboard `8877b29` — docs: rewrite CLAUDE.md against the built codebase; remove docs/ do-not-read stub
- pds-dashboard `0aa7564` — chore: delete Next-tooling-generated AGENTS.md; fold its docs pointer into CLAUDE.md
- pds-dashboard `d0a0cbd` — docs: soften AGENTS.md note — regeneration is fine, no need to re-delete

**Decisions made:** Rule numbers in both repo CLAUDE.md files are **frozen** — code comments in both repos cite rules by number ("Rule N"), so numbers are never reused, reordered, or repurposed; new rules append (added to the STATE.md decision ledger as #16). The four new Android feature areas are documented as *conventions*, not new numbered rules — everything inviolable about them is inherited from Rules 4/10/17/19. Dashboard Rule 15 documents reality: no generated Supabase types exist (typing is hand-rolled; `src/lib/types/database.ts` reserved if ever adopted). Version headers and changelogs dropped from both repo files — git history is the changelog; each carries a "last verified against code" line instead. The frozen v3.9 planning record is single-homed in `pds-planning/historical/`; WORKFLOW.md retired outright (recoverable from pds-android git history; its load-bearing Room migration-test procedure is inlined in the Android CLAUDE.md). Remaining STATE.md work items keep their original numbers (2–6) because the new dashboard CLAUDE.md cites items 3 and 4 by number.

**Verified:** Docs-only session — nothing touched the tablet, so no hardware verification applied. Verification was mechanical against the code: historical-banner byte-identity vs git HEAD; zero dangling references in both rewritten files (spike-records / WORKFLOW / DO-NOT-READ / PROD-PENDING / byte-aligned all gone); all 57 classes/constants named in the Android file and all 20 paths + 23 exported symbols named in the dashboard file exist in source; all 18 Android compliance-precision tokens present (both stop indices, null-at-terminus, both terminus discriminants, the flash timeline, the 22mm constants, undroppable locking, the verbatim account-status strings); the corrected `audio_version` wording verified against the function code (`enqueue-render-job` mints the version at enqueue from the `updated_at` snapshot; `audio-render-worker` stamps it into `routes.audio_version` at render success, index.ts:297–301).

**What's next:** STATE.md item 2 — the driver-controls / audio-output slice, starting with commit 1 (audio-output hardening: AudioFocus, Bluetooth-disconnect fallback + persistent driver warning, silent keep-alive ticker). All design forks are pre-decided in the decision ledger; the build should not re-litigate them.

**Banked / open:** `src/proxy.ts` (~line 10) stale Phase-B gating comment added to the STATE.md bank — it joins the two stale UI strings, and all four known-stale dashboard text sites are now catalogued in the dashboard CLAUDE.md's "known-stale text" gotcha. The repo-side `pair-device` rate limit was confirmed in code as 10 attempts/15 min per IP, but the *deployed* function's shape remains unverified — that is item 4's production audit. Generated Supabase types remain an unbuilt option with the path reserved.
